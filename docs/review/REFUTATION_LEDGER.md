# Refutation Ledger — PR #3 review loop

Purpose: single source of truth for review candidates that were **refuted or consciously accepted** during this branch's review loop (Codex R1–R21, claude-glm rounds GLM1–GLM37, property-harness rounds proph-1..3). Re-open an entry ONLY with new evidence (empirical repro, vendor-doc proof, or a demonstrated production path) — not by re-deriving the same hypothetical. Reviewers: read before flagging.

## Behavior decisions (test-pinned or vendor-documented)

- **glm38 round decisions** (fix round over the 12 surviving findings of code-review round 38 — /code-review max, ledger-filtered at the caller: 3 of the 15 emitted findings dropped as ledger-covered re-flags — the zai directory's missing 200-envelope recording is glm18-4's adjudicated status-only design ("do not re-flag the twin's omission"; reopen only with a live OpenAI-route capture), the setLogprobs(false) typed rejection is glm4-4 pinned and glm37-2 scope-corrected (the decision record exists; at most a docblock-rationale alignment note), and the per-surface guard_settings_save re-execution is the GLM2 #8 neighborhood deferred by user triage and re-closed by glm31/glm32's scope-decision precedent; 10 fix commits glm38-1..10, one adjudicated carve-out (glm38-11, ledgered below), then a two-lens verifier pass — independent correctness + security agents over the full 10-commit diff — verdicts in the round footer):
  - **The floor sweep owns the 8.0+ function vocabulary PHPCompatibility 9.3.5 cannot see** (glm38-1): fdiv() (8.0+) fataled the NAN guard pin on the composer-declared 7.4 floor — the locked PHPCompatibility predates several 8.0+ functions and has no availability sniff for them, so phpcs-compat passed silently. ToolchainSmokeTest sweeps the phpcs-compat tree set (bin, connectors, shared, tests — mirroring its vendor/dist/tools and tests/fixtures/data exclusions) for a curated 8.0+ call list, fail-loud with file:line, non-vacuity-guarded (the guard caught its own empty sweep during bring-up: tests/fixtures/data does not exist in the tree, and the realpath-false prefix check excluded EVERYTHING — the exact vacuity class the assertion exists for). array_is_list stays UNLISTED by design: the SDK polyfills it on the floor (glm31-9) and the harness canary pins the polyfill loads. A name joins the list only with a demonstrated 7.4 fatal and no polyfill. Comments included in the scan: prose naming the call shape gets rewritten, not exempted (the sweep flagged its own docblock's first draft).
  - **The PHP<8.1 reflection-invoke guard is at every drive site** (glm38-2): the chokepoint net pin and the diverging-JsonSerializable pin (protected createRequest()) and the entry-rejection lockstep pin (private static) threw ReflectionException OUTSIDE their catches on PHP <= 8.0 — glm29-1's and glm14-7's invariants were unpinnable on the declared floor while the guard idiom sat unused ten times elsewhere in the same files. Empirically differential-verified on real 7.4.33 in docker: pre-round both reflection sites error exactly as reported, post-round all four floor-affected pins (including glm38-1's NAN pair) green.
  - **An Api-key SUBCLASS refuses typed at the protocol wrap, and the probe judges the exact class BEFORE the funnel** (glm38-3, the round's trust-boundary defect — fail-OPEN sibling of the glm14-5/glm34-6 adjudicated foreign shape): wrap() rebuilt any ApiKeyRequestAuthentication subclass from getApiKey() alone, silently stripping whatever authenticateRequest() override the wiring carried (an org header, key rotation) — bare Bearer auth upstream, 401, no local diagnostic. The registry's instanceof gate accepts subclasses (the SDK never produces one: fromArray() returns new self, the class is not final), so the trigger is third-party wiring — the population glm14-5 adjudicates, but this shape failed open where the foreign shape fails closed. Both halves required: wrap()'s plain-class requirement went EXACT (get_class identity; the subclass throws the binding-family RuntimeException with its own fixed value-free message — the wiring class name is caller-controlled and never interpolated), and resolve_probe_authentication() returns null (the opaque disposition: inconclusive, nothing flies, nothing persists) for the subclass shape BEFORE the funnel — without the early return the funnel's new refusal landed in the Throwable catch and LAUNDERED into the ladder fallback (the subclass's readable key flying as the plain-wrapped fallback while the doomed rejection persisted as its verdict — the glm16-1 cross-credential shape). Mutation-tested: reverting only the probe half, the wiring flies and records. effective_key() still reports the subclass's own key through the raw read (glm34-6's design: readable material, no flight); the discovery path lands the refusal in ZaiDiscoveryCache's Throwable catch (miss marker + fallback, fail-closed); the glm24-10 label pin counts 3 rejection sites now (supersession documented at the pin). BOTH-LENS-VERIFIED, every leg empirical: model generation throws pre-transport with zero HTTP requests; the directory plants the miss marker and serves the plan fallback; no verdict can persist (record_definitive_verdict's one call site is reachable only after a flight, and nothing flies for a subclass); the probe-half mutation reproduced the laundering end-to-end a second time independently. Security-lens NOTE, ledgered here because it is a REAL behavior change on the TWIN the round did not plan: the early return lives in the SHARED base, so the zai (OpenAI) surface's probe went opaque for subclass wirings TOO — verifier-reproduced pre-round (a subclass flew the zai probe, the queued 403 persisted its invalid verdict) vs post-round (nothing flies, configured-pending). Strictly fewer flights, no bypass; the subclass still flies and still records through the zai surface's generation/discovery routes (that surface has no wrap — by design), so the invalid-verdict channel survives there. Consequent channel note: a comma-bearing SUBCLASS key can no longer reach authenticateRequest() on zai_anthropic, so glm26-1's definitive-invalid channel is now unreachable for the subclass shape (the opaque inconclusive instead) — fail-closed for flight; the definitive channel is unchanged for every plain/ladder credential.
  - **The token-limit payload names the STASHED wire value** (glm38-4): the finish-reason walk re-read effective_max_tokens() at parse time, so a mid-request setMaxTokens() (the vendor ModelConfig is mutable; a PSR-18 middleware or plugin callback capturing the model inside send()) made the message and typed accessor name a limit the request never carried — violating glm19-7's own "name EXACTLY what the wire member carried" contract. The params build stashes $wire_max_tokens beside the array it rode into; the max_tokens case reads the stash with a null fallback that keeps a pre-first-build hand-driven parse total (every lifecycle parse follows the build that stashed). effective_max_tokens() stays the ONE resolution rule; its docblock states the timing split (build-time truth vs parse-time stash). Mutation-tested: pointing the parse site back at the live helper fails the new pin on exactly the finding's shape (999 reported where the wire carried 4096).
  - **The registry's endpoint column IS each settings class's ENDPOINT_CLASS — pinned per row** (glm38-5): the settings→endpoint pairing was owned twice (the settings classes' ENDPOINT_CLASS, which AbstractPlanRegionSettings::handle_settings_change() sweeps through, and the SURFACES rows' endpoint column, which uninstall.php sweeps through) with no tie — a one-sided pairing edit split the two invalidation worlds and a stale 12h discovery catalog silently survived one of them (the silent-strand drift class the registry's own header records twice pre-glm20-4). glm37-3's key-set template, per-ROW pairing identity (stronger than set equality): row[i]'s endpoint === row[i]'s settings::ENDPOINT_CLASS, non-vacuity-guarded, and the constant read fatals loudly on a settings class that stops declaring it. Mutation-tested: a swapped endpoint column fails the pin at row [0].
  - **The error/malformed channels ride AbstractSseAggregator** (glm38-6): $error/has_error() and $malformed_event/has_malformed_event() were byte-identical field+getter pairs in both aggregators although the base exists precisely to own the plumbing they share — any flag-contract change (a reset method, a new corruption channel, a getter rename) had to land twice or the surfaces silently drifted. Flags hoisted as protected channels, getters as FINAL ones (one body, both surfaces); each subclass keeps exactly its flag-raising sites, where the two wires genuinely differ (zai: the GLM7 #1 index channel and glm29-2's payload/declaration error signal; anthropic: flag_corrupt_event() and its declared-error mapping). The moved field docblocks' per-surface history preserved as compact notes at each old field site (the glm35-9 discipline — merging decision history orphans the ledger's citations). The harness reflection helper reads inherited protected fields, so every existing pin and the mutation-invariant property harness stayed green UNCHANGED (62 aggregator tests, 37k assertions; proph re-verified on 7.4.33 in docker).
  - **The unanchored-include judgment is per STATEMENT** (glm38-7): the per-literal foreach in wp_connectors_self_containment_violations() never read $literal_pair, recomputed the loop-invariant $anchored/$escapesUp probes from the same include text each pass, and appended the identical 'not anchored' violation N times for an N-literal include. No test pinned the duplicates (every existing pin is a ContainsString over the joined list); a new pin asserts exactly-once for the two-literal statement. The runtime-segment analysis still judges each literal's own segments.
  - **The content-block membership consults the vocabulary owner's once-flipped sets** (glm38-8): parse_content_block()'s per-block strict in_array scans over MAPPED_TYPES/KNOWN_UNMAPPED_TYPES (the response-parse hot loop both transports of a generation ride) became isset() on AnthropicContentBlocks::is_mapped_type()/is_known_unmapped_type() — the glm26-12/glm37-11 idiom, owned where the vocabulary lives. Differential-verified identical over the constant spellings plus the hostile edges (empty string, numeric-string coercions, NUL-bearing, whitespace/case variants); the glm34-8/glm35-4 lockstep pins untouched and green.
  - **The stop-reason consistency check reuses the build loop's tool-call presence** (glm38-9): the third full pass over the parsed parts (a getFunctionCall() getter call per part) reconstructed $has_tool_call, which the loop above already computed — its tool_use branch ('tool_use' === $part_data['type'] with a non-null part) is exactly the getFunctionCall()-carrying part through the callee's own contract (the tool_use arm always builds one; the text/thinking arms never do). The branch sets the flag; the re-scan is deleted.
  - **The probe CLI parses argv ONCE — the getopt() supersession** (glm38-10, adjudicated per the round's do-not-force rule and EXECUTED): the rewrite was sanctioned only if behavior-identical under every glm31-3/glm36-9 pin and a net deletion, and it was — the scan collects name=>value pairs as it validates, deleting the getopt() call, the glm37-9 composed spec, and the array-shape guard (~40 lines), with zai_live_probe_option() reduced to a plain defaulting read. The collected semantics are getopt's own, STATED: a name's first occurrence supplies its value, any repeat marks '' — exactly the non-string mapping the lookup applied to getopt's repeated-option array — so the ledgered glm36 residual diagnostics ('--plan coding --plan=general' rejects through the whitelist, value-blaming, loud-only) survive verbatim as stated code instead of inherited accident. Evidence: old-vs-new differential (exit code + both streams, byte-compared) IDENTICAL over 4,735 invocations — every 1- and 2-token combination of a 34-token hostile set (equals-attached, empty '='-values, bare '--', single-dash, positionals, empty strings, '--plan=a=b', '-h'/'--help' as values, case variants), the exhaustive 3-token space over a 14-token core, and 800 seeded random up-to-6-token invocations; all 17 probe CLI tests green UNCHANGED; the glm37-9/13 source pins pass with the spec half consciously superseded AT THE PIN (the order-independent ban stays as a guard against getopt's return); spot checks confirm the three ledgered valued-repeat shapes byte-identical; verified on 7.4.33 in docker. glm23-3's argv-absent shape is inherent to the scan (empty argv → empty map → defaults → key-lookup diagnostic).
  - **The refusal gate's lazy resolution is a CONFIRMED CARVE-OUT, not the glm37-8 scope edge** (glm38-11, adjudicated — no code change): the finding claimed the gate pays a redundant endpoint resolution per request; the short-circuit analysis (the round's required first check) refuted the premise. region_switch_pending() reads the FLAG before resolving (no flag → zero resolutions), the region_pending return PRECEDES binding() (the two resolutions never co-occur on matching shapes), and binding() runs only when a stored state array exists. Steady state: the healthy configured path pays exactly ONE resolution (unavoidable — the binding compare needs the current cache key) and the no-state path pays ZERO; an eagerly-threaded resolution would ADD cost to the no-state path, so threading is incorrect, not merely unprofitable. Verifier-CONFIRMED by instrumentation (for_current_settings call counts restored after): no flag + no state = 0; no flag + fresh stored state = 1; matching flag = 1; mismatched flag + stored state = 2 — the both-resolutions path is real, and is PERSISTENT-RARE rather than strictly transient (verifier nuance, ledgered): the flag settles only on a MATCHING credential fingerprint, so a flag fingerprinting credential A while credential B is wired can persist until the next region change, paying one extra memo-backed resolution (two option reads) per gated generation — within glm15-6's accepted per-consult cost. The null-default parameters stay for the settle check (a genuinely different consult at verdict time) and the gate (lazy by design). Residual, documented here: the && order puts the binding compare SECOND, so a fresh VALID stored state pays the one resolution per gated generation before the verdict test rejects; reordering the pure conjuncts would skip it but is a micro-optimization outside the adjudication's scope.

- **glm38 verifier-pass decisions** (two independent lenses — correctness + security — over the full 10-commit diff; every one of the 10 fixes' claims HELD, ZERO confirmed defects, both lenses' own mutation tests reproduced the round's findings independently): glm38-1 held (canary-planted sweep failure with exact file:line; PHPCompatibility 9.3.5 grepped — zero references to the listed functions; the NAN pin fails under an is_nan-removal mutation, so it exercises the GLM2 #4 rejection); glm38-2 held (13 reflection sites swept, all guarded inline or through openPrivateProperty(); pre-round test files re-run on 7.4.33 error exactly as reported); glm38-3 held on every leg (see the entry's verifier note); glm38-4 held (both TokenLimitReached sites traced — the context-window one carries a null payload by design; the vendor has no separate streaming entry, so every production parse follows its build on the same instance); glm38-5 held (mutation fails at the row-labeled diff; the owners enumerated — no third pairing site: the probe reads both columns from the same row, not an independent pairing); glm38-6 held (scratch override of a final getter fatals; 58 flag-raising sites untouched; the one method_exists identity check targets has_malformed_tool_input, which stayed per-surface); glm38-7 held (mutation reintroducing the loop fails the exactly-once pin; no count-threshold consumer exists anywhere — all three gates judge empty-vs-nonempty); glm38-8 held (the verifier's own 98-case hostile differential, zero divergences; $type is is_string-gated before both consults); glm38-9 held BY PROOF (the callee's arms enumerated: the tool_use arm always constructs the FunctionCall — a ReplayValidatedFunctionCall extends it — and no other arm ever sets one); glm38-10 held and EXTENDED — the correctness lens's own 88-shape differential (zero diffs, after catching its own vacuous first pass where the mise shim failed identically on both sides — detected via uniform rc=1, rerun with the real binary; a methodological note for future argv batteries), the security lens's 714 shapes (zero diffs), and the ROUND'S OWN EXHAUSTIVE differential completing after the verifier pass: IDENTICAL over 44,495 invocations (every 1-, 2-, and 3-token combination of the 34-token hostile set plus 4,000 seeded randoms; exit code + both streams byte-compared) — the full glm38-10 evidence base is 4,735 + 714 + 88 + 44,495 invocations, zero divergences. glm38-11 held by instrumentation (the counts above). Whole-diff sweep: every production hunk maps to a claimed fix; NO unclaimed behavior changes; no new defects. Security lens CLEAN across all areas: no new attack surface (the subclass refusal interpolates only the label constant; no get_class() or key material in any new message; ErrorMapper verified passing it as the 500 family), no TOCTOU between the raw read and flight (get_class is exact and immutable; the vendor plain class has no mutation vector), no verdict-poisoning path (no flight → no response → no recording), no billing-exposure change on the probe rewrite (the map keys provably come only from post-whitelist names), no new file/network/eval/construct in production code, and nothing new persisted — glm38-3 strictly REMOVES a possible verdict persistence. Security-lens residuals, all below the bar, ledgered: the zai-twin probe-opacity note (in the glm38-3 entry — safe direction, real twin behavior change); the floor sweep does not descend SYMLINKED directories and its non-vacuity guard is aggregate (per-tree silence if a whole tree vanishes — dev-gate hardening nits); the pre-existing reject_uncarriable_credential frame-argument exposure (unchanged, all paths caught); the correctness lens's UTF-8 preg_split note (latent, tree clean). Full suite green in default and random order (1303 tests, 42161 assertions, 2 skipped — the live-key gates; +6 tests/+14 assertions over glm37 = exactly the round's six new pins).

- **glm37 round decisions** (fix round over the 11 surviving findings of code-review round 37 — /code-review max; the caller dropped finding 1's behavioral half as the glm31 verifier round's adjudicated corner — "the corrupt-only-with-no-choices shape rejects through the no-usable-event channel, so no silent-zeroing shape is constructible" — keeping only its doc-drift half as new evidence; 11 fix commits glm37-1..11, then a two-lens verifier pass — independent correctness + security agents over the full 13-commit diff — whose every claim HELD and whose two below-the-bar notes are fixed as glm37-12/13):
  - **The falsy-option line is scoped to the wire-INERT keys** (glm37-2, the ledger correcting ITSELF): the old line ("Falsy unsupported-option values ignored … setLogprobs(false) etc. are neutral no-ops, not errors") contradicted the pinned glm4-4 (commit 81b04d2) — AdvertisedOptionGuard's `null !== $value` gate rejects ANY explicitly-set WIRE_FORWARDED value (presencePenalty, frequencyPenalty, logprobs, topLogprobs; the SDK parent forwards every non-null value verbatim, so pre-glm4-4 setLogprobs(false) really shipped `"logprobs":false`), while the `! empty()` gate keeps the no-op tolerance only for the wire-inert keys (topK, webSearch, output-\* — never forwarded, verifier-confirmed against the vendor builder). The stale line had just burned round 37's reviewer/verifier on a non-defect. Do not cite it to tolerate setLogprobs(false) or to flag its typed rejection.
  - **The corrupt-only usage corner is stated honestly and pinned** (glm37-1, doc-only over an adjudicated behavior): aggregated()'s choices gate (glm26-11) returns BEFORE the glm31-1 resolution, so a corrupt-only usage stream WITH choices flags malformed (glm31-1, unchanged) while one with NO choices frame keeps malformed_event false and rejects through the generic no-usable-event channel — the glm31 verifier round's adjudicated corner (fail-closed: nothing aggregates, no zeroed-accounting success constructible; the message is the generic one, never the validator's usage wording). The `$non_array_usage_pending` docblock no longer claims "fails typed like its non-streamed twin" unconditionally (the glm36-2 honest-corner precedent); a behavioral pin holds the channel in ZaiResponseMappingTest (verifier mutation-tested: moving the resolution above the gate fails the pin with the malformed-channel message). Do not re-flag the corner's channel divergence.
  - **MESSAGES_ROUTE_BY_PLAN's key set IS the MATRIX key set** (glm37-3): the route map had ZERO test references (the resolver provider enumerates today's four combinations; the zai twin has a flat generation route, so no cross-surface pin saw it) — a plan added to PLANS+MATRIX but missed in the map passed every guard and hit an undefined-index read → null into the strict-typed api_url(string) → uncaught TypeError at the first real generation. The lockstep pin (glm34-8's vocabulary pattern) asserts key-set identity plus that every route is a leading-slash path messages_url() appends for every matrix combination; verifier mutation-tested (a scratch MATRIX row fails the pin as the suite's ONLY failure).
  - **The availability gate hooks document their un-wired contract** (glm37-4 + glm37-12, the glm15-24 honest-docblock disposition): credential_gate_availability() and the directories' availability() hooks hand out FRESH instances (no transporter, no wired authentication); every current consumer is option/transient state (verifier-enumerated — nothing calls isConfigured()/probe() through a hook; the registry consult rides AbstractProvider::availability()'s memoized wired instance). The misuse the docblocks state is SILENT, not loud: the harness-booted verifier repro (fresh un-wired ZaiAnthropicProviderAvailability + env key) got isConfigured() TRUE, exactly one 60s miss marker planted, state not persisted, no exception. The answer is FALSE under region-switch distrust (glm21-2's carve-out — the first docblock form omitted it; the verifier proved the omission empirically and glm37-12 added the clause to all three statement sites; the marker plants either way). Loud-wiring was assessed and rejected (a throw at probe entry would also change the genuine registry path's semantics).
  - **The probe-miss sweep survives a quarantined endpoint child** (glm37-5): probe_miss_transient_ids() mirrors its discovery twin's guarded shape (GLM12 #10) — child class quarantined but the SDK-free base loadable → identities compose through the NEW parameterized AbstractZaiEndpoint::compose_cache_key() (cache_key() itself delegates to it; the glm18-11 formula-owner pattern one member down, so no hand-mirrored formula exists); base gone too → the bounded empty sweep (residue covered by the option-name-prefix sweep and the marker's own TTL). The equality pin (composed === instance cache_key(), every plan × region, both surfaces) is verifier mutation-tested, and the quarantine simulation composes byte-identical names on both surfaces. Residual, ledgered (both lenses): a MIXED-VERSION install (new settings children + pre-round base + quarantined child) fatals at the undefined method — the pre-round code fataled in the same scenario at the quarantined-child dereference, and the discovery twin has carried the identical shape since glm18-11; same fatal class, strictly narrower overall.
  - **The SDK cache neutralization is ONE trait both directories compose** (glm37-6): Support\NeutralizesSdkModelCache owns getBaseCacheKey/hasCache/setCache (self::class in the trait resolves to the composing class — per-surface key scoping preserved; verifier byte-compared all 8 surface × plan × region base keys against the pre-round inline formulas), parameterized by an abstract discovery_endpoint_class() hook pinned to the registry row per surface. The endpoint-scoping and PSR-16 poison-entry pins moved to the shared AbstractZaiModelDirectoryTestCase (each now executes exactly twice — verifier `--list-tests`-verified), so the anthropic surface runs them for the first time (glm36-6 had verified once and pinned nothing); the zai-local copies are deleted with the move documented at the surviving site. The glm36-6 invalidateCaches() residual is unchanged.
  - **effective_key() rides wired_or_null(), completing glm36-4** (glm37-7): the fourth guarded reader consult was the only unadjudicated one (the ledger had adjudicated ONLY resolve_probe_authentication() as correctly-not-folded); its catch(Throwable) laundered a reader's contract-violating non-RuntimeException into "nothing wired" with the ladder key then reported effective — the cross-credential shape glm34-6's raw-hook discipline exists to prevent. RuntimeException-width now, like the three glm36-4 sites; verifier three-state battery identical on both surfaces (unwired/wired/foreign), and no constructible production thrower exists (both concrete availability classes are final, neither overrides the hook — the fold's loud direction is unreachable until a contract violation exists, which is the point).
  - **One isConfigured() consult resolves its endpoint once** (glm37-8): binding()'s internal default resolution plus region_switch_pending()'s own paid the plan/region option reads twice per consult (ProviderRegistry consults availability on every request). The consult threads one resolution through binding()'s existing precomputed cache-key parameter (GLM10 #1) and region_switch_pending()'s new optional endpoint parameter (the refusal gate and the settle check keep resolving in place via the null default). Verifier: for() memoizes per class|plan|region, so the pre-round double resolution returned the SAME instance — no observable change; glm15-6's per-consult-read boundary untouched (within-consult threading, no memo).
  - **The probe CLI's option vocabulary and key-source guidance compose from single owners** (glm37-9/10 + glm37-13): zai_live_probe_long_options() drives the raw-argv whitelist, the getopt() spec (name . ':' composed per entry — the scan-passes/spec-drops shape, glm36-9's silent-defaults class, is structurally closed), and both diagnostics (Oxford prose through zai_live_probe_oxford_join()); the key-source constants (ZAI_PROBE_KEY_ENV_LADDER, ZAI_PROBE_KEY_FILE) drive the lookup loop, the usage sentence, and the no-key diagnostic. Source pins hold the compositions (verifier mutation-tested in both directions) with an order-independent spec ban (glm37-13 — the first form's `'surface:'`-anchored ban missed a reordered literal; the new one anchors on getopt() receiving a literal array, which the composed array_map( spelling cannot match). Verifier 51-case hostile argv battery against the pre-round script: every acceptance/rejection decision byte-identical (exit codes and both streams); the ONLY output diff anywhere is the usage's key-source sentence rewrap (two wrapped lines → one composed line, same text). The file-header docblock stays prose.
  - **The aggregator's event-name membership rides once-flipped sets** (glm37-11): is_declared_event()/is_content_block_event() answer isset() over array_fill_keys static memos derived from the LIST constants (the vocabulary owners stay the constants; nothing else writes the memos); the per-frame strict in_array scans are gone. Verifier: 76-comparison hostile battery (every PHP array-key-coercion edge) identical to strict in_array; proph slice green.
  - **glm37 verifier-pass decisions** (two independent lenses — correctness + security — over the full diff; every one of the 11 fixes' claims HELD, ZERO confirmed defects, the two below-the-bar notes fixed as glm37-12/13): empirical highlights — the gate-hook repro and region-pending carve-out (above); every new pin mutation-tested (route map, identity equality, both probe source pins — each fails under its targeted mutation); the PSR-16 poison-entry pin green on the anthropic surface (behavioral proof the trait intercepts the SDK layer, no trait-conflict); effective_key()'s fold traced through all four callers; the argv battery and event-set differential above; invalidateCaches() and the glm36-6 residuals untouched. Security lens CLEAN across all eight areas: no new attack surface (compose_cache_key's inputs are all class constants or settings-sanitized enums; the fallback can neither strand markers a sweeper misses nor delete wrong names), no credential/value leakage (no new string interpolates a value; the catch is the vendor RuntimeException subclass — the exact documented contract), no billing-exposure change (spec and whitelist provably one owner), reflection harness-only, the static memos bounded and constant-derived, no DoS delta, the ledger edit accurate in both directions. LOW residuals, ledgered: the mixed-version fallback edge (pre-existing class, discovery-twin parity) and the documented-but-silent gate-hook misuse (no current caller; loud wiring rejected as not free). Full suite green in default and random order (1297 tests, 42147 assertions, 2 skipped — the live-key gates; +9 tests/+47 assertions over glm36).

- **glm36 round decisions** (fix round over the 7 surviving findings of code-review round 36 — /code-review max, ledger-filtered: 6 of the 13 emitted findings dropped as ledger-covered re-flags at the caller — the glm34-5/proph absent-member usage tolerance (a message_delta without usage consolidating output_tokens 0), glm18's known by-reference-CALLEE scanner boundary, glm26-2's ascending-interleave tolerance (named in the proph allow-list), glm18's goto backward edge (phpcs-gated), the GLM2 #8 deferred duplicate-settings-errors neighborhood (per glm31's precedent, deferred items are closed by scope decision), and glm31-9's JsonShape forwarder design; 7 fix commits glm36-1..7, then a two-lens verifier pass — independent correctness + security agents over the full round diff — whose FIVE empirically-confirmed scanner laundering channels and one probe silent-default falsified or extended the round's own claims and are fixed as glm36-8/9, with one doc-drift class fixed as glm36-10):
  - **Every destructuring and dynamic write spelling refuses the include proof on BOTH write paths** (glm36-1, falsified in its first form and completed by glm36-8 — the verifier round's centerpiece): glm36-1 gated the PHP 7.1+ square spelling beside the map path's list() refusal and ported both to the plain-variable collector, but its statement-level anchor spared a bracket glued to '(' or a keyword ('([$f] = …)', 'if ([$f] = …)', 'return[$f] = …', 'foo([$f] = …)' — all empirically PWNED at HEAD), its '[^)]*' list span could not cross a nested paren ('list( list($a), $f )', 'list( ($a), $f )'), the foreach VALUE bindings ('as [$f]', "as ['k' => $f]", 'as $k => [$f]', 'as &$f') were invisible to both paths pre-round AND post-glm36-1 (the header's bracket group ends ')' or ',', never '='), variable-variable writes ('$$name', "{'f'}" braced) never spell the target textually (the masked view blanks the braced name), and the round's own '[^;]*' spans are quadratic — a ~4 KB bracket-run BURNER statement exhausts pcre.backtrack_limit, preg_match() returns FALSE, and the old truthy reads treated the abort as "no match," spending the call's budget before the real write was examined. glm36-8 closes all five: the anchor is GONE (a bracket group an assignment follows can only re-bind what it names), the list span is '[^;]*', a header-bounded foreach pattern (paren-bounded after 'as', so the SOURCE position and loop-body brackets never trip it) refuses bracket bindings on both paths, the alias pattern gains its 'as &' twin, one '$$'/'${' anywhere in the visible regions refuses every proof in the file (dynamic naming defeats static proof wholesale — glm18-18's rule), and EVERY refusal reads '0 !== preg_match(...)' with both preg_match_all walks refusing on FALSE — a PCRE abort is a REFUSAL, never a no-match. Documented over-approximations, all safe-direction (glm29-3's doctrine): an element WRITE keyed by the variable ('$rows[$map] = 1', a read of the variable as index) now refuses (the anchor that spared it was the anchor-bypass hole), a pure list() source read ('list($a) = $map') refuses, and any file using variable-variables at all loses its proofs. Word boundaries after the variable end the '$mapx' false-refusal class. 13 new laundering fixtures + the burner-abort pin (a pure comparison that must flag through the guard alone); the real tree stays clean under composer conventions.
  - **stop_sequence's non-string degrade is the LEDGERED envelope-metadata tolerance, stated honestly** (glm36-2, the round's first boundary adjudication): a present-but-non-string stop_sequence latches nothing and flags nothing on the stream (key-present null in additionalData) while the byte-identical non-streaming body's vendor pass-through carries the value verbatim — reproduced end-to-end on both channels by the round AND both verifier lenses (every SCHEMA-LEGAL input — string, empty string, explicit null — agrees on both channels; the divergence exists only for values the Messages schema types string|null). Accepted, not latch-and-flag: the member is non-load-bearing on both channels (zero plugin consumers), the stream side is pinned by the proph harness's allow-list entry, and the class is GLM1 #9's (message_start's model degrades identically, ledgered as non-load-bearing metadata) — re-opening proph's adjudication would need new evidence and the divergence predates it. The one DEFECT was the comments: both the latch site and aggregated()'s header claimed "envelope parity" the channels do not deliver on non-legal inputs — rewritten to the real contract, and a behavioral pin asserts the accepted divergence in both directions (stream null + unflagged success; body verbatim). Residual, ledgered (correctness lens): an ABSENT member leaves the stream's key present-null while the body omits the key — a pre-existing shape asymmetry outside the adjudicated non-string claim.
  - **The probe's missing-value check rides the argv consumption site; an empty '='-attached value is a missing value** (glm36-3 + glm36-9): the round-31 parser split one judgment across three passes — a first-occurrence-only array_search pre-scan, a blind sequential value consumption, and getopt — so a trailing bare repeat ('--plan coding --plan') was silently ignored into the full live round trip while '--plan a --plan' died blaming the value through the whitelist. One sequential scan applies the absent-or-option-led check when consuming a value (glm36-3); the verifier round then confirmed both lenses independently: getopt() silently DROPS an empty '='-attached value ('--plan='), so the option fell to defaults and a key-present run was billable on settings the operator never chose — glm31-3's silent-defaults class. The scan judges the emptiness itself (glm36-9), same option-named diagnostic, before the key lookup. All glm31-3/glm23-3 shapes stay byte-identical (help-first, register_argc_argv=0, typos, single-dash, positionals, '--'); values stay consumed-never-judged (the whitelists own them). Residual, ledgered (correctness lens): VALUED repeats ('--plan coding --plan=general') reject through the whitelist with a value-blaming diagnostic although every passed value is valid — loud-only (exit 2, no billable run), pre-existing getopt last-array semantics via zai_live_probe_option()'s non-string mapping.
  - **wired_or_null() owns the guarded auth-reader invocation** (glm36-4): the identical RuntimeException-guarded reader call was hand-copied three times in one class (refuse_generation, refuse_discovery, record_rejection_via_reader) — the GLM5 #11 divergence class the file's own docblocks name. One private helper maps the readers' documented unwired signal to null; verifier-confirmed control-flow-equivalent at all three sites (catch scope wraps exactly the reader call, as before; the glm14-5 one-credential-flies-and-binds invariant intact; no credential material reaches any message). The fourth guarded read (resolve_probe_authentication) is genuinely different (Throwable catch, narrowing inside try) and correctly not folded.
  - **The guidance-to-system-instruction merge rides the shared builder** (glm36-5): the append-with-blank-line-or-replace rule was duplicated verbatim in both models' guidance consults — the twin-drift class JsonOutputGuidance's single ownership exists to stop. One static merge_into_system_instruction() serves both surfaces byte-identically (verifier-differential-probed over non-empty/''/null/'0' instructions); the zai_anthropic emptiness guard stays per-surface and load-bearing (its consult answers '' with no schema configured; the zai surface's guidance is provably never ''). Each suite's composition pin requires the shared call and forbids the hand-rolled separator idiom.
  - **The zai_anthropic directory rides the SDK's ROUTE-AGNOSTIC base** (glm36-6, the round's second boundary adjudication): the in-code docblock justified direct interface implementation against the OpenAI-compat abstract's baked-in route assumptions — but the route-agnostic AbstractApiBasedModelMetadataDirectory (which the zai sibling already rides one level down) imposes none: its final list/has/get trio and its one abstract (an ID→metadata map, exactly what the deleted models_map() produced) are route-blind. The hand-rolled trio is gone; the consult is the parent's sendListModelsRequest(); the sibling's cache neutralization rides with it (hasCache() never serves, setCache() stores nothing, the base key stays endpoint-scoped) so the WordPress transient stays the single cache and the per-consult option reads (glm15-6's pins) stay per-consult — verifier-verified empirically with a counting PSR-16 stub (zero has/get/set through the directory; mid-process retarget swaps the catalog on the NEXT consult; a planted poison entry at the exact computed key is not served). The trait layering holds (the protocol wrap's insteadof and the aliased raw getter coexist with the parent's own trait compositions; the glm35-3 sweep pin green). The unknown-model rejection now rides the vendor trio's sprintf spelling — the per-surface wording divergence the finding itself cited as drift evidence is gone; the wording is covered by the pre-existing fragment pin (exception type + id fragment). Residuals, ledgered: the inherited invalidateCaches() is NOT neutralized (one DELETE of its own never-written key on a host-shared PSR-16 store — delete-only, nothing observed or poisoned, nothing auto-calls it); the rename's stale models_map() doc references were the glm19-13 class, fixed as glm36-10.
  - **The usage validator's member list and mode are REQUIRED arguments** (glm36-7): the defaults on failure_reason()/reject() baked the Anthropic surface's vocabulary and strict mode into the shared class — a future call site copying the shorter default-riding spelling onto the OpenAI surface would validate ANTHROPIC_MEMBERS against an OpenAI payload, find none of its keys, and answer null fail-open (the malformed count reaching TokenUsage's int-typed constructor as a generic TypeError). Both parameters are required now — GLM7 #8's parameterized-divergence idiom (glm30-4/glm26-7); a missing argument fails LOUDLY (ArgumentCountError, verifier-verified). All production call sites explicit; the glm20-6 exactly-one-call pin and the glm26-7 lockstep pin hold.
  - **glm36 verifier-pass decisions** (two independent lenses — correctness + security — over the full 10-commit diff; every confirmed defect fixed as glm36-8/9/10, every other equivalence and adjudication claim HELD): glm36-2 held (both channels' observables verified per shape; the harness allow-list entry covers the mutation class); glm36-3's own claims held (all repeat/value shapes, no scan-vs-getopt divergence — repeated options in every spelling combo reject through the whitelist; '[$x] ??=' is a parse error, so no compound-destructuring channel exists); glm36-4 held (catch-scope and three-site equivalence; no message channel); glm36-5 held byte-for-byte; glm36-6 held empirically (PSR-16 counting stub: zero cache ops, no cross-instance channel, retarget swaps next-consult, sweep pin green, invalidateCaches side-effect-free); glm36-7 held (reflection-verified signatures, no stale callers, loud failure mode). Security lens CLEAN beyond the scanner/probe findings: no new file/network/eval/credential constructs, no unescaped caller data in any new message, regex cost bounded by pcre.backtrack_limit (~35 ms worst observed — the impact was the abort-BYPASS fixed in glm36-8, not availability). Full suite green in default and random order (1288 tests, 42100 assertions, 2 skipped — the live-key gates).

- **proph round decisions** (NOT a review round — test infrastructure: the mutation-invariant property harness for both SSE aggregators, proph-1 702330d + the verifier-round hardening proph-2 7cf37e9, closing the silent-divergence/silent-loss class the loop chased shape-by-shape — glm23-6, glm28-2, glm31-1, glm31-2, glm33-1, glm34-1 were all members — as an invariant over the mutation space; ZERO production defects found: the class as pinned at HEAD holds under every generated mutation):
  - **The invariant** (tests/SseAggregatorMutationPropertyTest.php): for every (well-formed corpus stream × mutation operator × site), the aggregation is exactly one of CLEAN (identical output and flags), FLAGGED (output differs and a corruption flag rose — or a Throwable escaped, which FAILs), or TOLERATED (output differs, no flag, and the observation matches an EXPLICITLY allow-listed class with a ledger citation). NEVER a diff without flag and entry, never an escaping Error/TypeError. The allow-list is enumerated, not discovered — an unmatched observation fails with seed, operator, and reproducing stream; widening requires adjudication here. The payload comparator is TYPE-STRICT and MAP-ORDER-INSENSITIVE (maps by member set, lists in order — content order is semantic, key order is not; proph-2: the first form's loose != equated null/false/[] and classified real null-family flips CLEAN, verifier-reproduced 9x per 60k).
  - **The operator table** (18 operators, ledger-history mapped): cut-data-json / cut-data-only-frame / byte-inject-nul / byte-split-utf8 / truncate-stream-mid-frame → glm23-6 and glm34-1 (undecodable payloads, the data-only corner included); drop-data-line → glm23-1 (the cut declaration); drop-event-line → glm23-1/glm33-1 (the cut carrier); drop-type-member → glm33-1 (the typeless payload, frame-level and delta-level); member-wrong-type / member-absent / member-empty-string → glm26-3, glm28-2, glm29-4, glm31-1, glm31-2, GLM7 #8 (every member a reader touches); duplicate-frame / swap-frames → glm16-2, glm26-2; partial-then-valid → the proxy-cut continuation; unknown-enum / unknown-event-agreed → glm33-4, GLM7 #18, GLM1 #15; usage-corrupt → glm31-1, Codex R15 #1; drop-frame → the wire-integrity class. Coverage and non-vacuity gates fail loudly if an operator starves or a channel goes unwitnessed.
  - **The wire-integrity tolerance class** (both surfaces, the generic entries' foundation): SSE carries no per-frame sequencing or checksum field, so a vanished, duplicated, or reordered WHOLE frame is indistinguishable from a stream that arrived that way — the aggregators' charter is visible in-band damage (frames are merged "as received"). The Anthropic twin still flags lifecycle-frame drops/reorderings through its required serialized lifecycle (verifier-probed: 20 hand-built lifecycle damage shapes all flag PAST the generic entries — the class tolerates exactly the undetectable: same-block delta loss/dup/reorder, glm26-2's ascending-interleave, glm16-2's duplicate terminal). Entry-granularity variants (a vanished choices/tool_calls element) ride the same class.
  - **Proph-adjudicated tolerances** (each surfaced by the harness itself during bring-up, zero production defects): the GLM9 #1/glm33-4/GLM8 #4 stop_reason latch (ANY string — '' included — and the schema's explicit null latch as received; unmapped values reject typed at finish_reason_for()); the GLM4 #11/Codex R15 #1 usage classes (absent members default-zero, valid non-negative-int VALUES are transparent — the validator judges type and non-negativity, never counts); the GLM12 #9/glm34-5 empty shapes ({} usage is the null-usage normalization gateway form; an empty message object is the composite of individually documented absent-member tolerances); the GLM1 #15/glm13-4 unknown-type drops (the EMPTY string names no type — the streamed drop and the started-block delta tolerance apply verbatim); the GLM7 #1/GLM10 #13 index-value merge identity (the index TYPE is corruption-judged, the value is the wire's own addressing — sparse/non-zero-starting are the documented legitimate shapes); and the GLM5 #3 associative {} / [] collapse (an array-shaped delta/function/tool_calls/choices member with no recognizable members is the empty-chunk skip, not corruption — a NON-empty list of non-entries still flags).
  - **Cross-surface parity, pinned**: shared corruption shapes (undecodable data-only frames, decodable scalars, unknown agreed names, corrupt-only usage, non-string roles, duplicated terminals) must earn matching verdict classes on both twins; the ONE encoded divergence is the terminal drop — the Anthropic wire REQUIRES the message_start/message_delta/message_stop lifecycle (every aggregated() null path flags, Codex R8 #3/GLM7 #5/Codex R16 #1/glm35-1) while the OpenAI [DONE] is a terminal marker whose absence is undetectable (the choices-emptiness gate glm26-11; GLM5 #7).
  - **proph verifier-pass decisions** (two independent lenses over proph-1; all findings harness-side, fixed as proph-2 — no production change): the allow-list is SOUND against over-breadth (every generic entry probed with hand-built lifecycle shapes; the index entries cannot mask a merge collision — the operator's fixed value 5 against corpus indexes 0/1 cannot construct the collision shape, and the collision itself is the GLM10 #13 verbatim-travel pin anyway; anthropic corrupt usage kinds ALL flag through the shared validator, only valid-int value changes tolerate); the gates are structurally sound (CASES=1/abc/garbage go RED at the coverage gate; CASES=0 falls back to the FULL default — never a thinner run); determinism verified (per-surface reseeds, zero other mt_rand consumers in the repo, random-order suite green); 7.4-compat and phpcs clean. Fixed: the loose comparator, the mislabeled drop-type probe target ('data-only.0.type' left its entry unreachable), the DEAD decodable-scalar cut entry (removed with the proof: a cut damaging a CONTRIBUTING frame always leaves undecodable JSON and a cut of a non-contributing scalar changes nothing — decodable-scalar SKIP frames joined the corpora instead), the missing CASES ceiling (250k; a leaked soak var can no longer hang the check pipeline), and the seed clamp's float-cast overflow ((float)(PHP_INT_MAX-2) rounds UP to 2^63 — the +1 offset left the int domain and mt_srand threw; digit-length-exact now, SEED=0 honored). Residual, ledgered (security lens, LOW): the effective env CASES/SEED are visible only through the assertion count and failure reports — never printed on green runs — and a sub-default green run (roughly the 60–1999 band) is thinner without a recorded note; the coverage gate still forces ≥18 firing operators, and the fixed-seed parity battery runs regardless.
  - **Numbers**: default 2000 cases/surface rides composer check (~0.6 s of a 6.2 s suite; 1264 tests, 42032 assertions, green in default AND random order); soaks 20k and 50k per surface green TWICE (pre- and post-proph-2; 100k total mutations, 300040 assertions, ~17 s each); alternate seeds (0, 15- and 20-digit clamp probes, three fixed) green; hostile CASES probes red-or-full-default as designed.

- **glm35 round decisions** (fix round over the 10 findings of code-review round 35 — /code-review max, ledger-filtered: ZERO novel correctness defects, the round's one candidate correctness flag struck as a glm26-2 re-flag — the ledgered ascending-stops-over-interleaved-starts pre-existing tolerance, re-derived without new evidence; 10 fix commits glm35-1..10, then a two-lens verifier pass whose one confirmed finding is fixed as glm35-11):
  - **The zai_anthropic stream parse rides the malformed-event channel ALONE** (glm35-1): every aggregated() null return raises the flag first — the three null paths (no message_start, no stop_reason-bearing message_delta, no message_stop) each set malformed_event before returning, and the model checks the flag AFTER calling aggregated() — so the former separate 'No usable message event was received.' branch below the flag check was unreachable dead code on this wire (its required message_start/message_delta/message_stop lifecycle makes every unusable stream a flagged truncation — Codex R8 #3, GLM7 #5, Codex R16 #1). Deleted, the invariant stated at the malformed throw and the aggregator's @return, and source-pinned absent. The zai twin's null channel stays LIVE by design (an OpenAI choices-less stream is unusable without being malformed, a shape this wire cannot produce) — do not copy the branch back from the twin.
  - **Every canonical SSE segment a builder can emit rides the builder** (glm35-2, completing the glm34-10 conversion the round under-counted): 28 hand-spelled message_delta+message_stop tails, 4 content_block_stop frames, 3 message_start frames — each byte-verified against the builder's runtime output BEFORE the swap through a layout-independent matcher (the four source lines could sit in any junction arrangement, trailing-noise continuations included). Deliberately left hand-spelled: malformed fixtures whose damage IS the tail (stop_reason:5), canonical tails the builder cannot emit byte-identically (input-side usage members, usage-less deltas), the seven array-idiom compositions, and every delta-without-stop truncation fixture. Verifier: all removed literals collapse to six distinct shapes, byte-identical to the builders at the exact tuples used (10/10, key order and separators included).
  - **The protocol-trait pin derives its class set by sweep** (glm35-3): the pin hand-listed the three SDK-interfaced files, so the fourth class it exists for (embeddings/count-tokens) could never be seen — and the omission fails open in production (fallback_authentication()'s base default is plain ApiKey auth, which z.ai accepts). The set is DERIVED now: every class under connectors/zai/src implementing the SDK's own WithRequestAuthenticationInterface (directly or through any parent — class_implements includes parent interfaces) under this surface's ZaiAnthropic naming convention must compose SpeaksAnthropicMessagesProtocol (checked through the reflection trait chain, not source text), supply the raw hook, and carry no bespoke wrap override. A >=3 lower bound proves the sweep sees the real tree; a fourth class enters automatically. The reflection assertions are strictly stronger than the old source-substring checks (a bare `use` import satisfied those). Residuals, ledgered (verifier, both pre-existing pin shapes, not regressions): the override-forbidding regexes pin the two HISTORICAL wrap spellings — a novel wrap-free getRequestAuthentication() override escapes the lexical pin (the three current classes are backstopped behaviorally; a fourth class needs its behavioral pin on arrival), and the sweep's class gate is the surface's naming convention (a class under another prefix is invisible — the convention every class of the surface follows, stated in the pin's comment).
  - **The body parse's member rule rides the content-block table's BLOCK half** (glm35-4 + glm35-11, the verifier round's one confirmed finding): the text/thinking arms' hand-rolled isset+is_string probes are ONE hoisted guard reading AnthropicContentBlocks::STRING_CONTENT_MEMBERS — the map's own contract ("every consumer") — with the message derived from the closed constant keys, byte-identical to the per-arm spellings; tool_use keeps its identity/input arms, and the lockstep pin now asserts the model rides the table, forbids every hand-rolled isset spelling, and drives a behavioral member battery (absent/non-string/foreign-string member per entry, non-vacuous against MAPPED_TYPES). The verifier found the first form's lookup UNSCOPED: the table also carries the two DELTA names (the aggregator's delta arms read the same map in their own context), so a body content block TYPED as a delta name answered 'A text_delta block is missing its text member.' where the pre-round parse and the commit's own claim gave the switch's unsupported-type rejection — message-only (same class, 'content' channel, reject verdict on every input; a delta-named block WITH its member still hit the unsupported arm both ways). glm35-11 scopes the lookup to MAPPED_TYPES (the block half) and adds the delta-named-block shapes to the battery (absent member, and the member the unscoped guard would read PRESENT and string — derived from the table's non-mapped keys so a restructure cannot run vacuous; mutation-tested: the unscoped form fails with the finding's exact message).
  - **The map memo stores no digest nothing reads** (glm35-5): glm26-6 replaced the digest compare with a strict stored-list compare but left the md5 computed and stored on every rebuild — zero readers in src/tests/bin, a whole-list hash per catalog change for nothing, and a docblock still calling it "the entry's identity". The key, the md5, and the shape clause are gone; the class, orchestrator, and both directory docblocks state the strict-compare contract in present tense (history narration kept as history); the corrupt-entry test keeps its failOnWarning purpose (the string-only filter the warning once rode still runs). Memo identity semantics unchanged (the strict list compare decided alone since glm26-6; cross-surface isolation was never the digest's — the distinct cache-id prefixes own it).
  - **The availability children describe their aliasing, not the deleted mirror** (glm35-6, doc-only): both children opened with the pre-alias contract ("the four identifier constants below mirror the SDK-free settings layer … a consistency test pins the mirror") directly above constant-expression aliases that cannot drift — an instruction manual for reintroducing the two-list drift the aliasing made structurally impossible. The comments state the alias mechanism and its compile-time rename guarantee; the headers' "env/constant name" member listing (the KEY_ENV_NAME mirror glm31-6 deleted) is corrected to the refusal label, the actual fourth constant. Verifier: every changed line is comment/blank — no code hunk, no value drift (aliases cannot).
  - **DebugSettings registers through the declaring owner** (glm35-7): register_settings() reached the plugin-wide option group through the concrete first-surface child (PlanRegionSettings::OPTION_GROUP) although the group is declared on AbstractPlanRegionSettings and every in-file consumer uses the declaring owner — the exact reach-through glm29-14 eliminated from this same file's handle_enabled_change(). Verifier: OPTION_GROUP is single-declared ('zai_connector'), the child does not redeclare it — byte-identical registration, capability guard, sanitize callback, and show_in_rest unchanged.
  - **The stop-lifecycle walks ride the stopped-prefix count** (glm35-8): while block i is open no stop above i can have been recorded (a stop at j > i requires every index below j stopped, i included — glm26-2's non-LIFO rejection), so the stopped blocks are exactly a prefix: "every index below N is stopped" (glm26-2's walk) reads as stopped_blocks === N and "every block is stopped" (Codex R15 #2's message_delta gate) as stopped_blocks === count(blocks). The count increments at the ONE stop-recording site (set + increment paired); the per-block 'stopped' member stays (the duplicate-stop and post-stop-delta guards read it, glm26-9). Equivalence: the round's 41,066-sequence exhaustive ordering differential and 4,000-run hostile real-class fuzz, plus the verifier's 8,000 randomized/pathological old-vs-new streams comparing full private state, aggregated(), and all three flags — zero divergences. No desync path exists: a fresh aggregator per stream, blocks enter only through start_block()'s contiguous 0..N-1 keys, no unset, no reset.
  - **The directory suites' surface-constant twins ride one base** (glm35-9, partial by decision): the plan/region retargeting, unauthorized-fallback, and general-fallback tests were byte-identical twins in both directory suites except for each surface's constants — the glm22-6 drift class (a shared-discovery-rule change re-encoded twice; a one-suite edit left the other surface pinning the superseded contract while green). AbstractZaiModelDirectoryTestCase owns the four tests; each concrete suite supplies settings class, models/error body builder, and a LITERAL URL map through hooks (the URLs are the pin, never derived from the endpoint classes). Verifier: each inherited test is assertion-for-assertion faithful to both originals and runs exactly twice (once per concrete suite; the harness base loads after WpConnectorsTestCase and its filename sits outside the Test.php suffix); the one fixture-byte change — the zai general-fallback body now rides openAiErrorBody's fuller error envelope — is a 404 JSON error either way, discovery fails identically, and the assertion is catalog-fallback only. Deliberately NOT consolidated, ledgered as known debt: the twins whose bodies embed per-surface decision history (verdict recording, memo, parse shape: GLM7 #12 vs GLM9 #5, glm18-4's status-only recording divergence, their port orders) — merging those comments would orphan the ledger's citations (the glm30 comment-density refutation) and their assertion bodies genuinely differ. The remaining suite pairs (Settings, ProviderMetadataAndAvailability) stay hand-mirrored with the class named.
  - **anthropicModelsBody() derives last_id from the input like first_id** (glm35-10): the $previous accumulator threaded the foreach solely to emit last_id — mirroring the input list by hand, desyncable from the emitted 'data' entries under any future in-loop filtering. Both bounds derive from the input directly now; byte-identical for the empty, single, and multi-entry shapes (verifier-differentially checked); the non-list divergence is unreachable (test factory, every caller passes a literal list).
  - **glm35 verifier-pass decisions** (two independent lenses — correctness + security — over the full 10-commit diff; ONE confirmed defect, fixed as glm35-11 above; every other equivalence claim HELD): glm35-1 held (the three null returns enumerated, each flag-first; the model's check order traced; the zai twin's live null channel confirmed); glm35-2 held byte-for-byte over the six collapsed shapes; glm35-3 held with the sweep run live (the derived set is exactly the old three — ZaiAnthropicProvider, the settings class, the endpoint, and the request authentication are correctly excluded; class_implements' parent-interface inclusion and the class_uses parent walk verified; the regex covers final/abstract/plain declarations, readonly impossible on the 7.4 floor); glm35-5 held (zero digest readers repo-wide); glm35-6 comment-only verified line-by-line; glm35-7 held (single declaration, same string, no import issues); glm35-8 held over the 8,000-stream differential plus the writer-site enumeration (one paired set+increment, no unset, no reset, contiguous keys, fresh instance per stream); glm35-9 held assertion-for-assertion with --list-tests proving exactly-two execution; glm35-10 held differentially over empty/single/multi. Security lens CLEAN on every shipped area: the glm35-4 exception-message escape claim proven end-to-end (the is_string gate kills non-string types at the fixed-message throw before the lookup; the table's four keys are non-numeric strings no PHP array-key coercion can alias — probed empirically; $member is a table value; and no render path prints the channel unescaped — the settings and debug surfaces esc_html, the logger records no messages); glm35-1's deletion is fail-closed (a hypothetical unflagged null still dies at parse_decoded_message's is_array gate — a typed rejection, never a fabricated success); the aggregator counter introduces no DoS or parse-confusion surface (strictly less per-stop work); the register_setting change widens nothing; the md5 carried no security property (in-process, never persisted; surface isolation is the cache-id prefixes'). Three LOW test-code residuals, none a regression (the glm35-3 spelling/convention/autoload notes above; class_exists() autoloads the scanned classes — side-effect-free today, fail-loud if not, tests excluded from builds). Full suite green in default and random order (1261 tests, 29992 assertions, 2 skipped — the live-key gates; +1 test, +19 assertions over glm34).

- **glm34 round decisions** (fix round over the 11 surviving findings of code-review round 34 — /code-review max, ledger-filtered: the round's caller struck 4 of the 15 emitted findings as ledger-covered re-derivations before the fix round — the glm33-2 shape-probe cost (the residual documented at the site), the glm23-14 memo-accessor unification (do-not-unify BY DESIGN), the glm33-5 mirror vendor-upgrade desync (the C2 class adjudicated one round prior), and the 403-as-credential-definitive scope (glm13-6's pinned recording design; reopen only with a live model-scoped 403 capture). 10 fix commits, 1 doc-only fix, 1 conscious accept):
  - **An undeclared UNDECODABLE data-only frame flags, never silently drops** (glm34-1, the round's one reproduced correctness defect — the corner BETWEEN Codex R4 #3, glm33-1, and glm16-15): a data-only frame (no `event:` field, no glm23-1 pending declaration to reunite with) whose payload FAILS JSON decoding fell through consume_frame()'s `! is_object && ! is_array` branch returning nothing — R4 #3 flags only DECLARED names, glm33-1 only DECODABLE payloads (typeless), glm16-15 only decodable non-objects. The fix is the twin's own discipline (glm23-6): `json_last_error()` distinguishes the undecodable shape, flagged through flag_corrupt_event(null) in BOTH phases (the sibling declared-name check is termination-independent per GLM5 #18's one-pipeline rule; the trailing-[DONE] skip at :761 owns the one sanctioned post-terminal data-only spelling). Tolerances pinned and untouched: decodable scalars (including explicit `data: null`) keep the glm33-1 skip, an empty `data:` value is a cut payload under the same rule (the twin's own empty-data note), the glm23-1 split-form reunion settles BEFORE the branch (a reunited undecodable carrier flags through the declared-name sibling as before), unknown declared names stay ignorable, and the json_last_error read is this frame's own (the decode at :780 is unconditional and adjacent — no stale-state false positive is constructible; verifier-verified). GLM12 #15 call-site pin superseded to 8 at its site. Residual, ledgered: a PRE-terminal bare `data: [DONE]` now flags where it was silently dropped pre-round (the skip is termination-conditioned BY DESIGN — GLM10 #5's pinned shape is post-terminal only; a mid-stream [DONE] has zero capture evidence, is indistinguishable from a cut frame, and the zai twin never silently ignores one either) — fail-closed direction; a pinned tolerance note may be added if a live capture ever shows a benign mid-stream [DONE].
  - **Both zero-part rejections on zai_anthropic carry the marker exception** (glm34-2, the glm28-4 parity closed): the zero-TRANSLATABLE-part and zero-parts rejections in parse_decoded_message() threw plain ResponseException while the zai twin throws FixedMessageResponseException for the identical condition — so json_fallback_result()'s null-out catch (glm14-2) degraded the precise message to the generic stream error on the mislabeled-Content-Type path, the exact asymmetry the marker contract exists to prevent. Byte-identical message/family by construction (fixed() re-derives through fromInvalidData); @throws declared. Residual, glm32's diagnostic class: the thrown trace's origin moves one frame deeper.
  - **Content presence is judged with array_key_exists semantics** (glm34-3): the entry probe was the parse's last isset() holdout — an explicitly-present `"content": null` misrouted through fromMissingData while every sibling envelope member (type, stop_reason, usage) sends explicitly-null values through the invalid-data channel (the conflation the file's own GLM #8-era comment calls a bug on stop_reason). The split names the non-array PRESENT value with the raw-oracle probe's own reason string ('The message content must be a JSON array.'), on every transport (the streamed path's entry check IS this one — no raw oracle); the genuinely-ABSENT member keeps fromMissingData, both pinned. GLM8 #4's stop_reason explicit-null legality pin untouched (different member).
  - **The guard_wire_values() attribution-order finding was REFUTED as drift and fixed as DOC DRIFT** (glm34-4, no behavior change): both surfaces' compositions are EXACTLY glm20-8's documented pinned orders (zai: stop, identities, system, sampling, declarations, text; zai_anthropic: identities, stop, text, system, declarations) — the finding's central claim that the zai twin "carries the same independently-landed divergence" misread it (zai is stop-first, and its docblock honestly states the old-eager-order contract). What WAS wrong: the zai_anthropic docblock claimed "the segment ORDER matches the mapping order," which the composition never satisfied (stop_sequences MAPS after system; the segments guard per-kind blocks, not per-message interleaved) — the glm19-13 doc-drift class, and the stated contract that invited the re-derivation. Docblock and both glm20-8 comment halves corrected; do not re-flag the composition as drift without a change landing AFTER glm20-8's pins.
  - **The zeroed input_tokens shape is CONSCIOUSLY ACCEPTED** (glm34-5, no code change): message_start carrying NO usage member AND message_delta's usage carrying no input-side member consolidates to input_tokens 0 with no flag — absent-MEMBERS on both frames (not present-but-wrong-type, the glm31-1 class), each half individually a documented tolerance (the aggregator's property_exists gate — GLM6 #4 documents "a stream whose message_start carried no usage member (tolerated)"; the delta's has_input_side fallthrough — GLM6 #4's legacy shape), and the byte-equivalent NON-streaming body with no usage member is PINNED to parse zeroed (Codex R14 #5's documented default-zero tolerance, testValidAndAbsentTokenUsageStillParse). Present-wrong-type usage members already flag on both frame types through validated_usage_view/UsageValidator. Do not re-flag absent-member usage shapes on either transport; the fix would need speculative machinery the round's instructions forbid.
  - **effective_key() reads the RAW wired instance** (glm34-6, the glm16-1 discipline extended to the last availability reader): the wrapping getter laundered a foreign (non-Api-key) wiring into wrap()'s type-refusal RuntimeException INSIDE the reader's try — indistinguishable from "nothing wired", reporting the ladder key as effective. Output was identical today by STRUCTURE (ZaiAnthropicRequestAuthentication EXTENDS ApiKeyRequestAuthentication, so the wrap product passed the instanceof with the same key string; foreign wiring fell to the ladder both ways) — the raw read makes the states structurally distinct so a future distinguishing edit starts from truth; the catch's one sanctioned throw is the raw accessor's own unwired RuntimeException; the probe's FLIGHT keeps the one wrap funnel (resolve_probe_authentication() re-wraps after the raw shape check). Pinned behaviorally (opaque and wired states produce the ladder/wired pairs) AND by source (the base's ONE `getRequestAuthentication()` call is the flight funnel). glm14-5's opaque-nothing-flies invariant verified untouched.
  - **The wpdb::prepare() stub substitutes in ONE PASS over the original query** (glm34-7, harness-only): the sequential preg_replace loop re-scanned already-substituted content, so a bound value carrying a literal `%s`/`%d` consumed the NEXT argument's placeholder (core substitutes each placeholder exactly once, left-to-right; the review's demonstration reproduced and now pinned as a stub self-test). The direct walk spells glm20-9's NET encoding explicitly (quotes escaped, backslashes and `$` tokens verbatim — the addslashes/preg-replacement round trip's exact product), so the pinned dollar/backslash behaviors and get_col()'s LIKE-to-regex contract are byte-identical (32 curated cases + 200k fuzz, zero divergences outside the fix); the docblock's single-argument scoping is lifted. Residual, ledgered: a NUL byte in a bound value now lands VERBATIM where the old stub's preg replacement MANGLED it (the `\0` whole-match backreference splice — itself a bug); no harness caller binds NUL. The pre-existing `%%` divergence from core stays (core collapses to `%`; cosmetic, caller-free).
  - **The Anthropic content-block vocabulary is ONE table both switches ride** (glm34-8): Support\AnthropicContentBlocks owns MAPPED_TYPES, KNOWN_UNMAPPED_TYPES, and the glm15-21 STRING_CONTENT_MEMBERS map (promoted from the aggregator's private copy — the glm15-21 pin superseded at its site to the shared spelling). The body parse drops the unmapped trio through the constant (the hand-enumerated case labels are gone), and a lockstep pin drives the catalog constants BEHAVIORALLY (every mapped type maps on BOTH transports, every unmapped type drops on both — a catalog addition needs a fixture, so the pin cannot run vacuously) plus lexically (no hand-enumerated trio arms, no private map copy). The unknown-type DIVERGENCE (streamed drop vs body reject) stays the documented GLM1 #15/glm26-2 decision — the pin is about the vocabulary's ownership, not that boundary.
  - **Test model wiring rides HARNESS_MODEL_ID, never a catalog literal** (glm34-9): the eight factory-wiring sites spelling 'glm-5.3' beside a primeZai*DiscoveryTransient() call (whose default id IS the catalog-derived constant of glm29-10) were the catalog-refresh desync that re-opens the documented glm29-10 incident (the prime would serve the new CODING_MODELS[0] while the request asked for the stale literal, missing pre-transport). A sweep pin forbids any future `::model('glm-…'` literal in tests/; fixtures that LIST ids (models bodies, discovery seeds) are not wiring and stay free. Residual: the sweep is single-quote/no-space LEXICAL (the repo pin idiom's known over-approximation direction).
  - **The Anthropic SSE envelope boilerplate rides HttpResponseFactory builders** (glm34-10): anthropicStreamStart()/anthropicBlockStop()/anthropicStreamEnd() emit BYTE-IDENTICAL frames to the canonical hand-spelled forms (member order, quoting, trailing newlines, integer formatting — verified byte-for-byte BEFORE converting, then mechanically multiset-balanced by the verifier: all 118 removed hand-spelled message_start ids reappear verbatim as builder arguments, every stop_reason/token bucket exact, no assert/catch line drifted). 297 of ~330 envelopes converted across ZaiAnthropicResponseMappingTest (121 starts, 112 block stops, 64 end tails, 117 stranded `''` prologues folded; net −542 lines). Deliberately NOT converted: deliberately-malformed fixtures (their malformation IS the fixture), the framing suite's own fixtures (SseFrameBufferTest tests the framing itself), and the bespoke OpenAI chunk streams — their implode composition carries no envelope boilerplate beyond the `data: ` prefix a builder would only re-wrap (scope decision, not an oversight). Residual, ledgered: the builders' id/token inputs are implicitly ASCII (a `/`- or unicode-bearing id would emit valid-but-differently-escaped JSON through wp_json_encode; every current fixture id is ASCII-alnum/hyphen/underscore).
  - **sort_callback() compares memoized per-ID keys** (glm34-11, the glm26-12 flipped-set idiom): usort invokes the comparator O(N log N) times per catalog build and every invocation re-ran the two extraction regexes on both IDs; sort_key()'s static-local memo computes each ID's [version, variant] once. The ordering RULE is unchanged (directory newest-first pins plus a 3,000-shuffle differential including hostile ids — zero divergences). The memo's key universe is PROVABLY bounded to the ≤10 constant-catalog ids on every feeder path (the parser's chat filter and plan intersect, and the discovery cache's sound-row rule all gate BEFORE any sort; no discovery-fed or hostile growth is constructible).
  - **glm34 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff; ZERO confirmed defects, every equivalence claim and both adjudications HELD): glm34-1 held over a 21-case pre/post differential (exactly 7 divergences, ALL the new rule's class — undecodable mid/trailing, empty data:, deep nesting, NaN, invalid UTF-8, pre-terminal [DONE] — with the aggregated payload byte-identical in every case and every decodable tolerance identical); glm34-2 held (fixed() is byte-identical by construction; the only sanitizing catch on the surface is JsonFallbackResult's, which passes markers — the fallback outcome is the one intended change); glm34-3 held (only the explicitly-present null changes channel; the mislabeled-fallback outcome unchanged); glm34-4 verified ZERO non-comment changed lines; glm34-6 held (wrap-product instanceof equivalence is structural; opaque-wiring nothing-flies re-verified at the probe's early return); glm34-7 held over 32 cases + 200k fuzz with the divergence classes enumerated (the intended fix + the NUL class the old stub itself corrupted); glm34-8 held (the in_array gate sits between the is_string gate and the switch; the trio's only old handling was `return null`); glm34-10 held mechanically (multiset balance exact, commit arithmetic 121+112+64=297, all non-canonical envelopes stayed hand-spelled on both sides — nothing half-eaten); glm34-11 held (both helpers total, variant eagerly computed but only read on the old code's path). Security lens CLEAN across all six areas: no DoS surface (the json_last_error read is this frame's own — the decode is unconditional and adjacent; a 200k-frame flood within milliseconds of pre-round; 50k-deep data-only payloads fail closed), no value/credential leakage (both marker throws constant-fed; no key material reaches any message/log — effective_key's catch swallows silently and the verdict binding names the flying credential for every wiring state), no new file/network/eval/credential constructs (the only added I/O is the source-pin file_get_contents idiom and one read-only directory sweep; AnthropicContentBlocks ships under the glm31-8 all-src artifact pin), no benign frame class false-flags (comment/id:/retry:/ping/scalar/split-carrier batteries), and no vacuous test conversion (all converted sites are wiring args, never assertion values; exactly one assertion line changed in the mapping-test diff — the superseded source pin). Full suite green in default and random order (1260 tests, 29973 assertions, 2 skipped — the live-key gates; +6 tests over glm33 = the round's new pins). Process note for future verifier rounds: a mutation check that pre-loads an OLD harness class must define it BEFORE tests/bootstrap.php (which loads wp-stubs.php itself) and must confirm the class actually loaded — an extracted snippet missing its `<?php` tag silently degrades the check to a vacuous pass (hit and corrected by this round's correctness lens).


  - **An undeclared TYPELESS payload carrier flags, never silently drops** (glm33-1, the round's one correctness defect — HIGH, empirically reproduced by the review AND re-reproduced before fixing): a data-only frame (no `event:` field, no glm23-1 pending declaration to reunite with) whose decodable payload carries no top-level `type` member derived `$type = ''` and fell through dispatch_event() as an unknown type — the repro aggregated a SUCCESSFUL completion (`'Hello '`) with the carrier's chunk silently missing and all three flags false. glm21-1's adjudication presumed the type member present ("a column-0 `data:` line still carries the type member past the lost `event:` declaration"); the typeless variant — the other half of a cut declaration's split — was the gap, and the adjacent inconsistency confirmed it a gap rather than a decision: an undeclared object with a PRESENT non-string type already flagged (GLM9 #2's branch), only the ABSENT-type shape silently dropped. Every documented Messages streaming event carries a type member naming the event, so an undeclared object-or-LIST payload names nothing dispatchable: the gate (`! terminated && null === event_name && '' === type` → flag_corrupt_event(null)) flags through the ONE classifier (GLM12 #15: a frame declaring nothing → the malformed-event channel), the same silent-loss verdict glm23-1 gives cut declarations. Tolerances pinned BOTH directions: an undeclared SCALAR keeps its drop (it cannot be an event carrier; the zai twin pins the same decodable-scalar skip, glm23-6), a typeless frame after the terminal keeps the trailing-noise policy (a completed generation cannot lose content to it), the typed carrier keeps payload-type dispatch (GLM1 #14), declared unknown names stay ignorable, and the split-form reunion is untouched (a reunited TYPELESS carrier merges — its declaration rode the previous frame). Boundary, verifier-confirmed coherent: an empty `event:` spelling counts as ABSENT (GLM5 #8) so bare-`event:`+typeless flags, and `{"type":""}` names nothing dispatchable and flags — the unknown-STRING-type tolerance holds for every NON-empty string. The GLM12 #15 call-site count pin superseded to 7 at its site (the GLM10 #4 lesson).
  - **Object-typed tool arguments are judged by their SERIALIZED shape** (glm33-2, MEDIUM — empirically verified before fixing): the scalar rejection's `! is_object()` carve-out presumed every object encodes as a JSON object — false for a JsonSerializable returning a list/scalar (`json_encode` of one returning `array('Oslo')` is `["Oslo"]`), which passed BOTH shape rejections AND the replay oracle (the serialization is stable) and shipped `"input": ["Oslo"]` where Messages requires an object: the misattributed-upstream-400 class the Codex R4 #4/GLM2 #2 typed rejections exist to prevent, while this surface's own inbound parser rejects the identical shape response-side. An OBJECT-typed argument now rejects typed when a successful RAW encode does not lead with `'{'` — the sibling rule of the list/scalar rejections at the same funnel (message_part_block), message fixed and value-free. A FAILED encode stays silent: the replay guard below owns unencodability with its own message (its first branch proves it on the same value — verifier-confirmed byte-identical for INF-bearing and self-referencing serializers). The probe skips ReplayValidatedFunctionCall instances (the GLM12 #12 stamp): inbound acceptance proved the shape by construction (every stored input is object-shaped — ToolArgsObjectNess products and the aggregator's stdClass requirement). Residual, documented at the site: the probe re-runs per request build on the unstamped object-args subset — one serialization, the same per-build cost class the glm21-5 memo already accepts where the memo cannot help (rehydrated conversations miss by design). Verified NOT in the bypass class: ArrayObject/ArrayIterator encode OBJECT-LED (`{"0":"Oslo"}`) on 8.5 and real 7.4 in docker — the storage-encoding concern is the glm22-16 WALKER's, never the wire shape's. Verifier-confirmed adjacent verdicts: a JsonSerializable returning `[]` now rejects (pre-round shipped `"input":[]`, which the inbound parser rejects — the outbound/inbound asymmetry the fix closes), and a lossy-float LIST changes only its message (already rejecting pre-round through the replay guard; same class, same channel, strictly more accurate). A hand-built stamp wrapping such args bypasses the gate exactly as it bypassed the replay oracle pre-round — the ledgered glm19-3/14 stamp trust boundary, unchanged.
  - **The tool-id uniqueness scope divergence is CONSCIOUSLY ACCEPTED** (glm33-3, no code change): outbound duplicate tool-call ids reject across the WHOLE conversation (GLM5 #6's pinned scope, :1100); parse-time `$seen_tool_ids` is per-response (:1931+). The poisoning shape (an upstream numbering tool ids per response — `toolu_0` every turn — parses cleanly, then every later request throws pre-transport) is real in the wild but not fixable on this side: (i) NO documented vendor guarantee of cross-response uniqueness exists (checked; a real Anthropic-COMPATIBLE layer — Moonshot/Kimi's forum bug report — shipped exactly the id-reuse shape, so the trigger is constructible against compat endpoints); (ii) parse-side conversation scope is STRUCTURALLY IMPOSSIBLE (the parser sees one response body; models are stateless per request); (iii) relaxing the outbound scope would reverse the pinned GLM5 #6 semantics and ship ambiguous tool-result correlation (a turn-1 result "answering" a turn-2 call with the same id) that strict upstreams reject with a worse error — the finding's own evidence (the ecosystem treats reuse as a bug) is evidence the guard is doing its job; (iv) the failure is LOUD and typed (fixed-message InvalidArgumentException, pre-transport, nothing flies), never silent corruption. Reopen ONLY with a live z.ai capture reusing tool ids across responses in a conversation that is well-formed under Anthropic semantics.
  - **The unknown stop_reason hard-reject is CONSCIOUSLY ACCEPTED** (glm33-4, no code change): finish_reason_for()'s fall-through throws the fixed typed rejection for any unmapped value (:2582 post-round). Founding design, not drift: the rejection and its `@throws` declaration date to the surface's skeleton commit (9f48ee6) and survived 32 review rounds; the VENDOR PARENT behaves identically on its own OpenAI path (the SDK's parse throws `ResponseException 'Invalid finish reason "%s"'` on its default: branch — the zai surface inherits exactly that through glm19-2's rewrite family), so the two surfaces are symmetric; and mapping an unknown value to stop() would FABRICATE a natural-stop claim the wire never made (GLM7 #5's documented anti-pattern — the same reason aggregated() refuses to fabricate end_turn on truncation). Explicit null stays the schema-legal mapped case (GLM8 #4 pinned). The forward-compat cost (a future Anthropic value arrives as a loud typed error naming stop_reason) is the intended operator signal — each new value gets a deliberate mapping decision (refusal → contentFilter, pause_turn → stop). Reopen ONLY with a live capture of the z.ai compat endpoint emitting an unmapped stop_reason value (e.g. an OpenAI-style 'length' leaking through the compat layer).
  - **carries_json_body() resolves Content-Type through the vendor's own HeadersCollection** (glm33-5, latent LOW — fixed as behavior-tightening): the glm14-4 mirror read the exact-case `$headers['Content-Type']` key while Request::getBody() resolves the header case-insensitively (HeadersCollection → getContentType()), so a caller spelling it 'content-type' (legal HTTP) made the mirror answer false — the assembled array rode as `$data`, the vendor re-encoded the payload at send time through the JSON_THROW_ON_ERROR path the ride deleted, and getData() went non-null on a request whose pinned invariant says it carries the pre-encoded string. The mirror now builds the SAME HeadersCollection the Request constructor builds from the same array and reads its first value (Request::getContentType()'s own pick) — spelling divergence and any future SDK resolution change are closed by DELEGATION, not by a widened hand-mirror (the desync the review's C2 named). Verifier fidelity notes: a comma-bearing `'text/plain, application/json'` now answers false — the vendor's own getContentType() returns only the first comma-segment (the collection splits comma strings at construction), so the PRE-round mirror lied and the post-round one is the faithful copy; duplicate differently-cased spellings pick last-set-wins exactly like the vendor; production today always spells 'Content-Type' (the vendor parent hardcodes it), so canonical behavior is unchanged. Pinned across lowercase/uppercase/multi-value spellings plus the non-JSON and body-less-method negatives (reflection on the final class's private static mirror — the glm19-11 idiom); the ride itself stays pinned behaviorally by the glm14-4 test.
  - **glm33 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff; ZERO confirmed defects, every equivalence claim and both adjudications HELD): glm33-1 held over a 43-sequence curated pre/post differential (every divergence exactly the gate's class; aggregated content byte-identical throughout) plus a 30,000-interleaving seeded fuzz (3,357 divergences, ZERO unexplained — the classifier required silent-drop→flag with identical remaining state AND a typeless-object/list atom present); glm33-2 held over a 24-case reflection battery through the REAL message_part_block() pre/post (byte-identical except the intended rejections; failed-encode silence held twice — INF-bearing and self-referencing serializers both fall through to the byte-identical replay-guard message); glm33-5 held over a 16-shape mirror differential against the REAL vendor Request as ground truth plus end-to-end createRequest() rides (pre-round lowercase spelling left getData()=array with the vendor re-encode pending; post-round rides the string, canonical spelling identical pre/post) and a 44-shape duplicate-key sweep finding ZERO disagreements between mirror and vendor; all three gates' pins mutation-tested (deleting each gate fails its test — glm33-1's deletion ALSO fails the independent 7-site source pin); both adjudications' reasoning checked for holes (none — the structural/guarantee/loudness legs verified in source). Security lens: no DoS surface (400-deep and 1MiB payloads and 20k-frame floods all within milliseconds of pre-round, the flagging path not slower than the drop path; 50k pending declarations constant-memory), no value leakage (the fixed rejection message never carries argument data — pre-round the same value actually SHIPPED), no untyped-crash channel (throwing/resource-holding serializers behave identically pre/post; the caller-detonation class is ledgered trust-boundary), no error-channel inflation (flag_corrupt_event(null) cannot set has_error() or record an availability verdict), and no new file/network/eval constructs or credential material. Full suite green in default and random order (1254 tests, 29926 assertions, 2 skipped — the live-key gates); the one first-run BuildArtifactsTest zip-unlink error reproduced green in isolation and on re-run (the ledgered glm31 dist-artifact environmental conditional; the diff touches no build tooling).

- **glm32 round decisions** (fix round over the 2 surviving cleanup findings of code-review round 32 — /code-review max, ledger-filtered: the review's 10 finder angles produced 20 candidates, its own verification refuted 3, the gap sweep was empty, and triage dropped 13 of the 15 emitted findings as ledgered re-flags or in-code documented decisions — zero correctness defects; five angles came back completely clean):
  - **apply_delta()'s text/thinking arms append unconditionally** (glm32-1, the glm24-5 dead-conjunct class completing glm15-21): the text_delta/thinking_delta arms re-checked isset()+is_string() on the very member dispatch_event()'s has_string_content_member() gate — the glm15-21 map — had already proved present-and-string, and that gated site is apply_delta()'s ONLY caller (private on a final class; the glm23-1 split declaration/carrier reunites into the same pipeline; handle_trailing_event never reaches the arms) — the re-checks were dead by construction. The arms append unconditionally now; the per-arm block-type mismatch checks and the input_json_delta partial_json check are untouched. A source pin (ZaiAnthropicResponseMappingTest::testTheGatedDeltaMemberCarriesNoRecheckInsideApplyDelta) holds BOTH halves of the invariant — exactly one `$this->apply_delta(` call site, and no phpcs-spelled `isset( $delta->text )`/`isset( $delta->thinking )` re-check back inside — mutation-tested all three ways (a pasted text re-check, a pasted thinking re-check, and a second caller each fail the pin).
  - **JsonEncodeGuard::must_encode() rides encode(), the one oracle** (glm32-2, the glm30-6 micro class): the guard method hand-copied encode()'s json_encode-false check plus throw in the very class that exists to single-source the raw oracle. It calls self::encode() with the returned encoding discarded now — exception type and message byte-identical BY CONSTRUCTION (verified byte-identical over a 22-class hostile battery: INF/NAN, recursive graphs, invalid UTF-8, resources, 600-deep nesting); every must_encode call site was already a void statement call (EncodabilityNet's segments, both models' guard sites), and both mapping suites' behavioral message pins (14 zai + 12 zai_anthropic assertions) hold.
  - **Refuted/dropped at triage** (do not re-derive — every one is a ledgered decision or an in-code documented one; none offered new evidence): the inconclusive-/models not-configured shape → glm21-2 (consciously accepted; the reopen condition is a live capture of a valid key hitting an inconclusive 200) + glm13-2; the undecodable-data-frame flag → glm23-6; post-sentinel content discard → GLM6 #2/GLM5 #7/GLM7 #2; wrong-type member hardening on the zai aggregator → glm28-2 + glm26-3; the corrupt-only usage declaration → glm31-1; strict native-int index rejection → GLM7 #1 + glm21-9; the unreachable null-raw sub-path in parse_content_block() → glm19-3/glm19-14 (the unreachability IS the verifier-traced, pinned design; deleting it reverses the decision) + glm16-9; the guidance join split across the models → glm28-17/glm23-14 per-surface-halves discipline, guarded by glm23-2's cross-surface byte-identity pin; credential_binding()'s sha256 memo (raw key in the cache key) → glm15-6, the residual already recorded by glm30's verifier round; the probe CLI's three-pass argv parsing → glm31-3, round-31 verifier-fuzz-validated; the empty stop/tools unset from the wire → documented at the site (spec-faithful min-length-1 reading, twin parity); message_start id/model non-string silent skip → GLM1 #9 envelope parity, fail-closed through the SHARED consolidated-parse non-empty-id check (both channels reject byte-identically; a non-string model member silently drops — parity-pinned, non-load-bearing metadata); the wp_json_encode() harness stub returning raw false → documented in-file, latent (zero current call sites encode non-ASCII caller data; correctness encodes ride the raw oracle per GLM3 #4), and fail-closed rather than lossy.
  - **glm32 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff; ZERO confirmed defects, every equivalence claim HELD): glm32-1 held over a static only-caller proof (the gate predicate is truth-table-identical to the dropped check on every shape), 27+57 curated frame sequences plus 3000 seeded and 35,000 hostile fuzz interleavings byte-identical pre/post, and a dead-check instrument proving the deleted guards NEVER fired on any reachable input (zero trips across 20k fuzz sequences plus the named battery); glm32-2 byte-identical over the hostile battery (class, message, code), single json_encode per call, all callers already void. Full suite green in both orders (1248 tests, 29898 assertions, 2 skipped — the live-key gates); phpcs/phpstan/conventions clean. Residuals, both below the finding bar: must_encode's exception ORIGIN moved one frame deeper (the must_encode argument frame prints in both pre- and post-round traces, and the surfaces rewrite these exceptions to fixed messages anyway — glm19-2), and the re-check pins are LEXICAL (they catch the phpcs-spelled re-check; a differently-spelled semantic twin would evade — the repo's source-pin idiom, over-approximation direction safe).

- **glm31 round decisions** (fix round over the 10 verified findings of code-review round 31 — /code-review max, ledger-filtered: the review's 13 finder angles produced 31 candidates, 17 verifiers adjudicated, 11 refuted directly by this ledger; the caller also dropped the fork's `$ships_forwarded_values` candidate — the ledger's own Known-deferred GLM9 cut-over-cap item, no new evidence, a re-flag of a user-triaged deferral):
  - **A corrupt-only streamed usage declaration flags the stream** (glm31-1, the round's one cross-channel defect): a PRESENT non-array `usage` member skipped the merge silently, so a stream whose ONLY usage declaration was corrupt completed with zeroed accounting while the byte-equivalent non-streaming body rejects typed (UsageValidator's object rule — the zai lenient flag rescues exactly null, never a scalar). This is NOT glm28-2's "usage guards are untouched" tolerance re-flagged on new derivation: GLM7 #8's letter covers absent/null MEMBERS of a usage object and partial usage OBJECTS, never a present non-object member, and the empirical cross-channel divergence (glm18-4's class) was new evidence. The FIX is state-bounded, not unconditional, precisely because GLM6 #3's pin covers the other half: the member is remembered in BOTH phases (the glm15-14 one-predicate rule) and malformed_event rises at end of stream — after the post-[DONE] gap-fill — ONLY when NO valid usage member merged in either phase. GLM6 #3's pinned shape survives verbatim (a late "usage":"corrupt" after a valid merge stays superseding noise, generation completes) and is now pinned in BOTH orders (corrupt superseded by a later valid member; corrupt resolved by a trailing valid member); the non-streamed scalar verdict is pinned beside them (byte-equivalence both directions). The oracle capture rule is untouched (present AND an array, GLM6 #3).
  - **Fragments over a non-empty start input is corruption, not a merge** (glm31-2): the tool_use consolidation replaced the content_block_start-carried input wholesale with the decoded input_json_delta fragments — silently discarding start-carried arguments on a reporting-success stream, the class the AnthropicSseAggregator docblock itself names ("silently altered tool arguments on a corrupt stream must fail as a parse error"). Vendor-doc settled: the Messages streaming wire carries tool_use arguments as input_json_delta partial_json fragments over an EMPTY placeholder input at content_block_start (platform.claude.com's streaming guide shows every tool_use start with "input":{}), so both-present is nonconforming and ambiguous — flagged through the tool-input channel (glm20-2 precedence), return null. The adjacent boundaries are pinned and untouched: an empty-string fragment accumulates nothing and the start input stands (the pre-round no-op pin), and a start-block input with NO fragments still becomes the call args. A start input with no fragments and no deltas at all is glm28-4's empty-arguments shape — untouched.
  - **The probe CLI's parser rejects unknown shapes and answers help** (glm31-3): getopt() silently DROPS unrecognized options and the GLM8 #7 pre-scan knew only the three exact tokens, so --surfac=anthropic, -surface, a stray positional, and --help ALL fell to the defaults — and with a key present the probe ran the full live, BILLABLE round trip on the default surface reporting PASS (live-reproduced by the review; pre-existing on master, in-scope because this PR rewrote the parser). The fix is a SEQUENTIAL raw-argv scan: every option-led token must be a known long option (--surface/--plan/--region — this CLI's own flags, the same list the missing-value pre-scan walks), single-dash tokens reject (getopt reads them as undeclared SHORT options and drops them too), positionals reject (a value without its flag is the same silent-defaults class), and a space-separated option's VALUE token is consumed never judged (the whitelists own values; the missing-value pre-scan still owns the bare-option shape). --help/-h answers BEFORE anything else with a usage composed from the owners the validation rides (the built surface map for the surface list/default, AbstractPlanRegionSettings::PLANS/REGIONS for the spellings), exit 0, never reaching the key lookup. The surface map is built BEFORE parsing now (behavior-neutral move; the exit-3 no-facts path fires earlier). glm23-3's argv-absent shape unchanged (nothing to scan).
  - **An unreadable subdirectory converts the self-containment walk's abort to a named violation** (glm31-4): the shared walk aborted with an uncaught UnexpectedValueException — fatal, exit 255, under `composer check` AND `bin/inspect-artifact.php` — while the sibling unused-import scan has converted the same abort to a counted FAIL since glm17-17; build.php's catch already held (UnexpectedValueException extends RuntimeException). NOT a glm17-16 re-flag: that round's guard and pins cover the unused-import scan only — the round-31 finding named the asymmetry. The guard lives at the ONE shared walk (plugin-tools, the glm26-6 altitude), not the two call sites the finding named: the abort becomes a named violation with the partial scan kept, so every consumer's failure channel fires automatically (the gate's count, inspect's list, build's refusing RuntimeException). Pinned with the chmod-000 shape (functional opendir probe; glm17-16's root-defeat note — skip where the host opens through it). The inspect CLI's SEPARATE php -l iterator is NOT covered by this decision (out of the round's scope; it iterates a freshly-built zip's extraction).
  - **DebugLogger's read-append-write is CONSCIOUSLY ACCEPTED as racy** (glm31-5): two concurrent PHP-FPM workers with debug logging enabled can interleave get_option→append→update_option and one worker's row is lost (last writer wins with a COMPLETE array — a dropped diagnostic row, never a corrupted one, in an off-by-default 50-row ring buffer of method/redacted-URL/status/duration). The loop's speculative-race bar: the code is PRE-EXISTING with zero diff on the file in this PR (the PR adds a second producer through existing call sites), WordPress's option store carries no row locking and core itself accepts this read-modify-write class for option-backed state, and there is no empirical repro or production loss report. Do NOT add locking machinery without a real loss report; the boundary is documented at log() (the glm15-24 honest-docblock pattern).
  - **The availability classes carry NO KEY_ENV_NAME mirror** (glm31-6): the two mirrors were production-dead (env/constant resolution is AbstractPlanRegionSettings' own ladder — the availability layer never read them), the glm19-10 test-only-alias class. Deleted with glm19-10-style no-redeclaration pins at both consistency sites; the settings class is the one owner. The OTHER four availability identifiers stay (STATE_OPTION/REGION_PENDING_OPTION/KEY_OPTION/REFUSAL_LABEL are read by the availability layer's own code).
  - **action_links() derives the settings page from the registry** (glm31-7): the plugin-row Settings link hand-named PlanRegionSettings::PAGE_SLUG while every other settings-page consumer (zai.php boot(), DebugSettings) rides ZaiSurfaces::settings_classes()[0] — the last settings-page reference outside the registry's reach, and the OLD behavioral pin asserted the slug VALUE (a stranded literal would still pass it). The link derives the owner now and the pin reads the owner's slug the same way the production code does.
  - **The artifact must-ship pin sweeps the source tree** (glm31-8): the hand-enumerated eight-class list was checklist-in-code (a third surface's classes, or any renamed/dropped file outside the eight, could silently miss the zip) — the pin now asserts EVERY connectors/zai/src file ships, with a non-empty-sweep guard against a vacuous root. The non-src literals (asset, uninstall, LICENSE) and the root-php-files pin stay.
  - **JsonShape::is_list() rides the NATIVE array_is_list()** (glm31-9): the SDK polyfills it for the 7.4 floor (files-autoloaded: vendor/composer/autoload_files.php wires src/polyfills.php) AND its own hot paths call it on every request (PromptBuilder — the plugin's own prompt path — and the OpenAI-compatible parse the zai surface extends), so every functional SDK installation provides it; every JsonShape consumer is SDK-dependent code. The hand-rolled range-over-count idiom is deleted and forbidden EVERYWHERE (the GLM8 #13 extraction pin SUPERSEDED at its site per the GLM10 #4 lesson — it used to require exactly one hand-rolled copy in JsonShape.php); a canary pins the harness context loading the function (named failure, never an undefined-function fatal in a generation). phpcs-compat passes with no ignore (PHPCompatibility 9.3 does not flag the call at the 7.4 floor).
  - **Micro decision** (glm31-10): the is_object_shape() docblock's duplicated summary line + stray '/*' opener (a paste artifact) — deleted; no runtime effect.
  - **Refuted/dropped by the round itself** (do not re-derive): the review fork's `$ships_forwarded_values` finding — the ledger's Known-deferred GLM9 cut-over-cap item re-flagged without new evidence (the user's ledger instruction governs: deferred items are closed by a round's scope decision, not re-tracked as findings).
  - **glm31 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff; ZERO confirmed defects, every fix's equivalence claim HELD): glm31-1 held over a 19-interleaving usage battery (no interleaving flags with a valid usage present — $usage never returns to null after a merge; the corrupt-only-with-no-choices shape rejects through the no-usable-event channel, so no silent-zeroing shape is constructible); glm31-2 held over a 17-shape tool-input battery (only a replayable non-empty stdClass start input can trip the new check — every other start shape already flags at start_block and leaves the EMPTY placeholder the check reads as unset); glm31-3 held under 16 hand-crafted argv shapes plus a 300-shape canary-key fuzz — no disclosure, no bypass to a defaults run, every pre-round accepted invocation still accepted (two cosmetic non-defects ledgered here: a space-form value spelled exactly `-h`/`--help` answers help exit 0 — help-first is the documented design, no billable run possible; bare `--` now rejects where getopt ignored it — consistent with the no-positionals decision); glm31-4's re-indent proven whitespace-only, partials-kept demonstrated creation-order-aware, and the hiding attack (violating files inside a chmod-000 subdir) FAILS CLOSED at all three consumers (the abort is itself an unconditional violation; nothing packages); glm31-9's swap differentially verified over 18 fixed shapes plus 200k randomized key batteries on 8.5 AND 100k on real PHP 7.4 in docker with the polyfill loaded — zero divergences; the whole-diff sweep found NO new runtime channel of any kind. Security-lens process note: the lens's own register_argc_argv=0 test satisfied the key lookup from ~/.config/z.ai/api_key and ran ONE unintended live billed generation (~90 tokens) — glm23-3's ledgered argv-absent shape, pre-existing and unchanged in exposure (the round's scan strictly REDUCES accidental-billing exposure for the argv-present case). Residuals, both below the finding bar and unchanged in exposure: the walk-abort violation prints an ABSOLUTE path where sibling violations are repo-relative (dev-tooling output; strictly less than the pre-round's printed stack trace), and the argv-absent residual. Unexplained observation, recorded for completeness: the fix-round's own suite runs printed 29894 assertions where all post-verifier runs (13+, both orders, with and without dist/ artifacts) print 29895 — identical test/skip counts, zero failures, not reproducible in either direction, nothing in the diff accounts for it (likely one environmental conditional assertion); not a defect.

- **glm30 round decisions** (fix round over the 5 surviving findings of code-review round 30 — /code-review max, ledger-filtered: 10 of the 15 raw findings were re-flags of ledgered decisions and dropped with citations; then the 10 verified cleanup findings the round's 15-cap cut were re-emitted and adjudicated — 2 fixed as glm30-6/7, 8 REFUTED, listed below):
  - **setUp() erases every SDK credential a previous test wired** (glm30-1): two leak channels held a wired credential beyond its test — the registry's per-provider authentication map (bindModelDependencies() stamps it onto new model instances; any later registerProvider() re-applies it to the provider statics) and the credential on AbstractProvider's process-wide availability/directory instances (set through the registry call AND the direct setRequestAuthentication() several suites use). The SDK has no unset API; the reset is reflection writes onto the trait's NULLABLE storage ('unwired' is its default), and the per-instance nulling must go through the DECLARING class — reflection does not see the private trait-composed property from a concrete surface class (empirically: hasProperty() false on ZaiProviderAvailability, true on the abstract base that composed the trait). Transporter and memo state stay untouched. Pinned by a polluter/depends pair wiring both channels; mutation-tested (disable the reset → the dependent test fails on the carried credential). Note for future harness docblocks: PHPUnit parses `@depends` ANYWHERE in a docblock, so prose mentioning the annotation in a test's comment produces "invalid @depends annotation" skips (hit and fixed during the round).
  - **The HTTP-error catalog names each surface by its CARD NAME** (glm30-2): safe_http_message()/to_wp_error() take the API display label as a required parameter; each model declares HTTP_API_LABEL on the card-name chain (the settings layer's PROVIDER_LABEL — glm24-2's direction). 'z.ai API' (PROVIDER_LABEL . ' API') keeps the OpenAI surface's messages byte-identical to the old hardcoded wording; the Anthropic surface passes 'z.ai (Anthropic API)' verbatim (no ' API' suffix — the card name already carries one; that asymmetry is the two card names' shape, documented at the constants). The traits the models compose (ThrowsSafeHttpErrors, SafeGenerationBoundary) read the constant via static:: — a composer without it fatals loudly (the GLM6 #12 discipline). The to_wp_error() fallback ("The %s request failed.") is the ONE wording change on the OpenAI surface, on an unpinned path; both surfaces pin that the 429 catalog text names them.
  - **Every non-2xx status outside 3xx/4xx/5xx is RuntimeException** (glm30-3): the vendor ResponseUtil's exact bucketing — a final 1xx informational (the Response DTO admits 100-599) was mislabeled RedirectException → zai_redirect_error by the old <400 fall-through; 6xx+ joins the invalid class too (previously ServerException). The SDK RuntimeException family carries a status-only, label-bearing message onto the CODE_ERROR mapping. Pinned by a status-family battery (100/101/301/307/400/422/500/599; no 401/403 rows — they record the definitive rejection (glm13-6) and would refuse later rows' generations pre-transport).
  - **TranslatableMessageParts owns the translatable-part predicate** (glm30-4): the shared shape (thought-channel drop, function-call/response translation) lives on Support\TranslatableMessageParts; the ONE deliberate divergence — glm28-4's pinned empty-text tolerance — rides a parameter each model passes from its private EMPTY_TEXT_TRANSLATES constant (zai: true, the empty-string text part maps to a wire text entry; zai_anthropic: false, the Messages block rule drops it). Both pinned behaviors survive byte-for-byte; do not "unify" the flag away and do not copy the predicate back into a model.
  - **OPENAI_TOTAL_DERIVATION_MEMBERS is the named, pinned derivation vocabulary** (glm30-5): an absent total derives prompt+completion (GLM12 #6) from the validator's named constant, deliberately NOT computed from OPENAI_MEMBERS — whether a new validation member counts toward the total is a wire-semantics decision at the constant, never array arithmetic (mirroring the Anthropic side's separate INPUT_SIDE_MEMBERS summation vocabulary). A pin asserts the sets agree today, so an OPENAI_MEMBERS change breaks it and forces the constant's conscious revisit; do not replace the pin with runtime derivation.
  - **Micro decisions** (glm30-6/7): wired_credential() owns the wired-ApiKey pair construction effective_key() and effective_for_authentication() shared (the GLM9 #14 extraction had left the mapping duplicated — the GLM5 #11 divergence class); the zai directory's auth-reader closure is ONE factory (wired_authentication_reader()) — a closure factory, NOT a method callable, because the consumers invoke the reader in their own scope and the closure captures the directory's scope so the raw getter (glm26-4) stays callable without widening visibility (glm21-14's discipline ported to the two zai spellings).
  - **Refuted this round — the cut-cleanup re-flags** (do not re-derive): **Plugin::PROVIDER_CLASSES mirroring ZaiSurfaces::SURFACES** (glm20-4 stands — ZaiSurfaces must stay SDK-free loadable (Codex R2 #3/GLM8 #11), its own header states the fixed direction, and a provider column cannot live there; the lockstep pin is the guard); **the tool-schema memo's and JsonOutputGuidance memo's config-identity triggers** (glm16-6/16-16 and glm21-7/16 stand — belt-and-braces, NOT load-bearing: PHP's STRICT array compare judges object elements by IDENTITY (=== requires the same instance; only == compares properties), so the strict declaration-list compare alone catches every value-equal identity re-set — the config trigger only releases a memo whose entries are for the very objects the new config still carries, pure by identity (glm16-6), so either state is correct. The round's first refutation text had these semantics BACKWARDS ("strict compares by value"); verifier-corrected empirically. Removing the trigger would still change the documented release discipline for zero gain — the ledgered design stands); **credential_binding()'s sha256 memo machinery** (glm15-6 stands — the full-tuple-keyed memo IS the pinned memoization-boundary decision, 2-3 derivations per request documented); **the $lenient parameter on UsageValidator::failure_reason()/reject()** (GLM7 #8 stands — the flag is the pinned legacy-semantics carrier, the parameterized-divergence idiom glm30-4 itself used; glm26-7's one-owner composition keeps per-surface arguments as arguments); **the json_fallback_result()/guard_assembled_params() wrappers** (glm14-4/15-7/16-7/16-8 stand — they are the per-surface composition sites, and their call shapes are source-pinned at ZaiRequestMappingTest/ZaiAnthropicRequestMappingTest); **the 63-line BuildsJsonOutputGuidance trait** (glm28-17 + glm25-3's parents-differ precedent stand — 41 of the 63 lines are the decision documentation, and inlining reverses the extraction); **comment-density reduction** (the glm-round references in comments ARE the decision trail this ledger cites — "documented at the pin/site" is the loop's own supersession mechanism (the GLM10 #4 lesson); no convention caps comment density, and stripping the references orphans every ledger citation).
  - **glm30 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff, glm30-9 fixes the one confirmed defect): every fix's equivalence claim HELD — the credential reset reaches EVERY SDK credential store (vendor-wide enumeration: one credential property, three composing hierarchies, exactly the two static caches the walk iterates; no test's green depended on the leak — the suite is green in both orders WITH the leak eliminated); the OpenAI catalog byte-identity held across all branches rendered against the pre-round strings; the status-class mapping matched the vendor for every 100-599 status with no vendor catch intercepting the new RuntimeException and glm13-6 recording still preceding the family branches; the predicate extraction was case-exhaustive; ErrorMapper's signatures NARROWED (required parameters) and no new attack surface opened (the reflection is harness-only, unreachable from connectors/). One DEFECT, both lenses' doc-audit class: the round's OWN refutation text for the memo config triggers stated PHP's strict array compare backwards — corrected above (glm30-9). Security-lens residuals, both pre-existing/out-of-charter: the env re-wiring after a map reset is the ladder's intended semantics (ZAI_API_KEY never set in-process, pinned by glm14-9), and credential_binding()'s in-memory memo KEY contains the raw credential (glm15-6's capped, process-lifetime design — flagged only because in-memory credential enumeration was in scope).

- **glm29 round decisions** (fix round over the 15 findings of code-review round 29 — /code-review max, ledger-filtered, every finding verified before fixing; then a two-lens verifier pass — independent correctness + security agents over the full round diff, verdicts in the round footer):
  - **The net's fall-through ships the RE-ENCODED artifact** (glm29-1): `EncodabilityNet::encode()`'s `return '';` was REACHABLE — jsonSerialize() is user code whose output can diverge between invocations (full-payload oracle fails, attribution walk's member re-encodes succeed), and glm14-4's ride shipped the empty string as the body with generateTextResult() reporting success (reproduced on the shape through the zai chokepoint). The fall-through returns `JsonEncodeGuard::encode()`'s string now: a payload no invocation can serialize still rejects typed (identical message and channel); the deterministic case returns at the first encode as before. Pinned: the diverging double's invocation count (2) and the shipped body's decoded shape. Do not re-flag the second serialization on the failure path — it only runs after a failed first encode.
  - **The zai aggregator's error-event channel is the twin's, in this wire's spelling** (glm29-2): the signal is a PRESENT `error` member on a decodable event object (absent or explicit null keeps the member's absent semantics — the glm28-1 one-check idiom) OR an `event: error` DECLARATION (the twin's GLM7 #4 discipline: the payload's condition cannot un-declare it — a truncated or undecodable error event still counts; the declaration check runs BEFORE any payload judgment, and only 'error' is read — every other event name keeps the historical ignore per GLM7 #18/GLM1 #14). Both phases flag identically (the glm15-14 one-predicate rule), the model checks has_error() FIRST (the twin's position; the GLM8 #5 JSON fallback cannot fire — its live scenario produces no data: field lines), and the wording is the surface's own ('The chat-completions stream contained an error event.'). glm23-6's tolerances SURVIVE and stay pinned: a scalar `data: null` keeps its non-event skip, and a choices-less object WITHOUT an error member keeps its skip — the round's conscious divergence from the twin (which has no choices shape on its wire). Do not re-widen to payload-agnostic error flagging.
  - **Interpolated double-quoted literals are RUNTIME segments in the include proof** (glm29-3, SECURITY — the round's proof-machinery fix): ONE quote-aware predicate (`wp_connectors_literal_is_interpolated()`: any '$' in a DOUBLE-quoted literal; single-quoted '$' IS the filename) serves every dynamic judgment — the main scan's unanchored flag (which the old '${'-only dynamic test SUPPRESSED, leaving unanchored '${}' includes flagged nowhere), the anchored-escape walk's early bail, the expression reasons, and the PSR-4 shape guard; `wp_connectors_include_runtime_segments()`'s blanking KEEPS interpolated double-quoted literals visible (they surface as unresolvable runtime segments), so the assignment-mediated route (`$path = __DIR__ . "/sub/$x.php"; require $path;`) also rejects. Deliberately over-detects escaped `"\$x"` (pinned as the conservative direction — a visible false violation beats a silent traversal hole). The old `'${'` spelling test is source-pinned absent. Do not re-narrow the predicate to live-interpolation-only (backslash-parity precision): the over-detection is the design.
  - **A present-but-wrong-type tool_use id/name flags at the Anthropic stream channel** (glm29-4, the glm28-2 parity): start_block()'s accumulator ternaries still null non-string values (the corrupt value never merges) but the guard flags malformed_event first — the channel the zai twin uses for the same shapes, replacing the coincidental-downstream-catch (parse_content_block()'s generic identity rejection) the twin's own comment says corruption must not rely on. An absent member or explicit null keeps isset()'s absent semantics AND its parse-time glm18-2 rejection (pinned both directions). The glm20-2 flag precedence is untouched (tool-input still outranks malformed-event when both trip).
  - **The empty-prompt rule is ONE guard rule** (glm29-5, glm18-3's maxTokens-parity class closed for the member): `RequestShapeGuard::reject_empty_prompt()` serves both surfaces; the zai call sits LAST in validate_request() (the twin's validate_message_order() position — a multi-invalid request keeps naming the member the twin names), and the twin's inline copy rides the guard (byte-identical message). One inherited pin in AbstractZaiSurfaceRequestMappingTestCase executes on both surfaces (the glm22-6 shape). The vendor parent's message assembly is unchanged — the rule fires before it.
  - **The redaction pin rides the WIRED key** (glm29-7): FakeSecrets::apiKey() draws per call, so the old pin compared against a secret never on the wire (the round's verifier leaked a hex fragment of the wired key through all three checks). ONE draw wires (the new `model_with_key()` hook, one-line delegates per suite) AND asserts; a canary first proves assertRedacted() flags exactly this key instance when present. Correctness-verifier round: the canary's FIRST form caught AssertionFailedError around a fail() INSIDE the try — PHPUnit's fail() throws that exact class, so a regressed (no-op) assertRedacted passed the canary too (empirically demonstrated); the canary records a flag in the catch and asserts it OUTSIDE (mutation-tested: a no-op assertRedacted fails both surfaces' tests), so the pin cannot go vacuous again without failing its own canary. The availability_class() hook on the smoke base was RETIRED in glm29-9 with the derivation it duplicated (KEY_OPTION/STATE_OPTION read off the settings class — glm24-1's owner direction); the twins' pins keep naming settings+provider classes.
  - **The probe's discovery evidence is the named cache id** (glm29-8): `discovery_cache_id()` on the endpoint base, never `$discovery_transient_ids[0]` — the pair list serves the delete loop only. A reorder/grow of discovery_transient_ids() can no longer read the '_miss' marker (literal true) as live evidence.
  - **ONE round-trip runner owns the live acceptance sequence** (glm29-9, extends glm28-11 one layer down): tests/harness/ZaiLiveRoundTrip.php owns the ordered steps (option writes, wiring, endpoint evidence, availability with the R13 #5 state delete + GLM2 #6 marker clear + R17b definitive rule, discovery with the R8 #6 transient clearing + live-vs-fallback signal + GLM12 #11 evidence URL + glm29-8 named id, generation with glm19-9's preferred id/fallback + Codex R17's empty-output rule, and the state-plaintext check), emitting every report line through a caller-supplied callback (byte-identical CLI output). The CLI judges the structured outcome through exit codes; the PHPUnit base through assertions — each reconciled to the stricter side (the skeleton now demands a definitive verdict and LIVE discovery; the CLI gains the plaintext check). SUPERSEDED PINS (the GLM10 #4 lesson, documented at the pins): the smoke-base owner-constant derivation spellings (`$settings::OPTION_PLAN`/`$availability::KEY_OPTION`/`$provider::PROVIDER_ID`) now pin the RUNNER with `$settings::` spellings; the probe's `$surface_facts['settings']::KEY_OPTION`/`STATE_OPTION` and the `'live ' . $endpoint->models_url()`/`->generation_url()` evidence spellings re-target the runner; GLM7 #14's Throwable-catch count re-targets the runner with a shell-wide Exception ban; glm28-11's "derivation statements pinned once in the base" is superseded to "once in the runner". The probe's facts table keeps all five columns (the map mirrors the registry+SDK facts; the runner consumes settings/provider/endpoint and re-derives PROVIDER_ID at wiring). Verified LIVE on both surfaces (PASS, identical output, 2026-09-07; re-verified through the glm29-16 shell guard). Do not move a step back into either shell.
  - **Micro decisions** (glm29-10..15): the harness model id is ONE catalog-derived constant (`HARNESS_MODEL_ID = ZaiModelCatalog::CODING_MODELS[0]`, the glm19-9 idiom) feeding the wiring and all three discovery-prime defaults — HttpResponseFactory's response-body model echo stays (the server's reported value, a different fact); the eight inline Anthropic error envelopes ride `anthropicErrorBody()` (the message-less `{"type":"forbidden"}` fixtures stay inline BY DESIGN — the factory cannot emit a member-less envelope); one harness CapturingTransporter($body) (the OpaqueAuthentication pattern); the plugin boot facts ride ZAI_PLUGIN_FILE/ZAI_PLUGIN_BOOT + loadZaiPlugin()/bootZaiPluginAndInit() next to loadPlugin() (the four suites' private bootPlugin() copies had DRIFTED — same name, load-only in three suites, load+init in the fourth); DebugSettings' equality check calls the declaring AbstractPlanRegionSettings owner; the wpdb::get_col stub THROWS on unrecognized query shapes (the silently-compliant direction is gone; no test relied on the silent empty).
  - **normalize_empty_object_members() does not descend stdClass nodes — CONSCIOUSLY ACCEPTED** (glm29-6, the round's one PLAUSIBLE correctness finding): a HAND-BUILT mixed tree (array root embedding an explicitly-cast or non-assoc-decoded stdClass node that itself carries a hand-written `[]` at a schema position) ships that `[]` verbatim — mechanically confirmed (the walk's root guard returns non-arrays untouched; the per-keyword gates descend is_array members only). Accepted on reachability: both schema entry points type-gate the ROOT to array (FunctionDeclaration's `?array`, ModelConfig::setOutputSchema), a stdClass root cannot enter; the REALISTIC embed — a json_decode'd subtree — needs no normalization at all (`{}` arrives as stdClass already, pinned) and its `[]` members are genuinely-declared lists the JSON source stated, so descending and converting them would MISREAD the caller (GLM8 #6's `[]`-means-`{}` charity exists for PHP's array conflation, which decode products do not have — pinned as decode-truth, `properties":{"a":[]}` ships verbatim); and no plugin, SDK, or DTO-gated production writer builds the hand-cast form (the tree's json_decode sites are all response/args parsing, never schema construction). The residual is pre-GLM8 #6 diagnosis quality (the misattributed upstream 400) on an unconstructible-by-production input — never silent corruption of a valid schema. Do NOT re-flag without a production writer of the mixed form; the boundary pin lives in ZaiAnthropicRequestMappingTest (testStdClassSchemaNodesPassThroughWithDecodeTruthPreserved).
  - **glm29 verifier-pass decisions** (two independent lenses — correctness + security — over the full round diff, glm29-16 fixes their two confirmed defects):
    - **The canary pattern records a FLAG and asserts it OUTSIDE the catch** (glm29-16, both lenses confirmed): a `$this->fail()` sentinel inside a `catch ( AssertionFailedError )` block is swallowed by its own catch — PHPUnit's fail() throws that exact class — so the glm29-7 canary's first form passed green with a deliberately no-op assertRedacted (empirically demonstrated by both lenses on PHPUnit 9.6.36). The fixed form is mutation-tested (a no-op assertRedacted fails both surfaces' tests). The round's OTHER try/fail tests are sound — their catch classes (ResponseException, InvalidArgumentException) differ from AssertionFailedError; do not add a canary that catches the assertion-failure base class around a fail() sentinel.
    - **The CLI shell guards the round-trip call** (glm29-16, the security lens): ZaiLiveRoundTrip::run() carries the live key in its argument list, and zend.exception_ignore_args=Off (the engine default) prints argument frames in uncaught fatals — an uncaught Throwable anywhere inside the runner would print the key in the stack trace (the pre-round top-level frames carried no arguments; no realistic trigger exists today — the steps are try-wrapped and probe() catches internally — but the GLM7 #14 strict-types-Error class is real). The shell names the failure as a safe FAILED line and exits 1; the trace never prints. PHPUnit's own trace renderer strips arguments, so the smoke shell needed no change.
    - **Verifier-refuted this round** (do not re-derive): every glm29-3 bypass vector the security lens could construct — heredoc/nowdoc interpolation, double indirection ("$path" holding an interpolated string), sprintf/implode-built paths, mid-path stream wrappers, every backslash-parity spelling of escaped dollars (the any-$ rule covers all live spellings because every one retains a literal '$' byte), the escaped-quote and apostrophe-containing literals whose shapes MIS-CAPTURE under the naive quote-pair regex (both caught by the escape-aware runtime-segment layer — the layering is load-bearing), the PSR-4 autoload shape with an interpolated literal, and the mixed static+interpolated anchored form — all reject at HEAD, with the pre-round behavior demonstrated SILENT on the closed holes. Residuals are pre-existing or out of charter: single-quoted '$' text (correct — never interpolates), file_get_contents/fopen/eval outside the include gate's charter (zero call sites in the shipped connector), symlink include targets (zero symlinks in the tree; a filesystem fact, not a source-scanner model), uppercase REQUIRE (the ledgered phpcs-gated boundary, re-verified), and the server-echoed-key-in-error-body channel (byte-identical pre-round; the WP_Error surface redacts, which glm29-7 pins).
    - **Also verified holding** (both lenses, no defects): the glm29-1 ordering question (in the divergent case the shipped invocation-3 body contains serializer outputs the walk's invocation-2 never encoded — but the walk applies ENCODABILITY only, never content validation, and no branch of the net ever content-validated the encoded string; the invariant "the flown body is the output of the very invocation whose success proved it encodable" holds in every branch); an 11-shape glm29-2 battery (no false positive on any legitimate frame, including the choices+error combination, other event names, and the GLM1 #14 capture shape); glm29-5's guard position and non-deadness (the vendor parent ships 'messages': [] verbatim with no earlier rejection); glm29-9's full step-by-step equivalence against the pre-round probe (option-write reorder inert by glm15-6's per-consult reads; every old exit=1 path covered by the new mapping; the PHPUnit assertions strictly stronger); glm29-10's constant (=== 'glm-5.3', PHP 7.4-legal empirically); glm29-11 byte-identity; and glm29-13's 1:1 call mapping (44 call sites).

- **glm28 round decisions** (fix round over the 16 actionable items of code-review round 28 — /code-review max, ledger-filtered; the round's six UNVERIFIED candidates were adversarially verified first: four CONFIRMED and fixed as glm28-2/4/15/16/17, two REFUTED and ledgered below; is_object_shape stayed deferred by disposition; then a two-lens verifier pass — independent security + correctness agents over the full diff — with ONE confirmed defect, glm28-1's first form, fixed as glm28-22, and every other equivalence claim HOLDING: a 4000-stream merge_tool_calls fuzz, a 21-shape tolerance battery, the malformed-event/zero-part/fallback precedence battery, a pre/post non-streamed model differential, a 3996-URL normalize_base_url corpus, split-walk and hidden-include differentials, SseFrameBuffer interleavings, the probe ladder's six wiring shapes, and pin mutation tests on the cap/composition/lockstep pins):
  - **A present-but-wrong-type member is corruption on the zai aggregator** (glm28-2, extends glm26-3): every member guard that silently skipped — non-string `delta.content`/`delta.reasoning_content`, non-array `delta.tool_calls`/`delta`/`choices` (pre- AND post-sentinel identically, the glm15-14 one-predicate rule), and the tool-call fragment's `id`/`type`/`function`/`function.name`/`function.arguments` — flags malformed_event and skips the merge. The harmful repro: `"arguments":123` as a stream's only arguments fragment fabricated a successful no-argument call. Tolerances kept: absent/null members keep the absent-semantics skip (GLM7 #8, glm23-6's object-without-choices), the ''-string content fragment keeps its merge-nothing skip, the usage guards are untouched (GLM6 #3/GLM7 #8 pinned), and `event.id` non-string stays the benign symmetric skip. Do not re-widen any guard to silent.
  - **A generated turn the outbound mapper cannot carry rejects at parse time on the zai surface** (glm28-4, the GLM3 #1/GLM5 #4 parity): zero parts, or no translatable part (thought-channel text maps to null on this vendor mapper), throws the marker FixedMessageResponseException in parseNonStreamBody() — the one funnel all three transports ride, so the fallback keeps its typed channel (glm14-2). The verifier reproduced the poisoning end-to-end: the empty assistant Message replays as `{"role":"assistant","content":[]}` verbatim (vendor prepareMessagesParam emits the empty content list; tool_calls is omitted when empty; thought parts map to null). Tolerances kept: an empty-STRING text part maps to a wire text entry on this surface (unlike the twin's empty-text drop its own rule mirrors) and stays a successful parse — pinned; glm6-15's empty-arguments tool calls are translatable parts.
  - **normalize_base_url() strips to a fixpoint, longest suffix per iteration** (glm28-5, latent — unreachable through runtime input): a doubled suffix leaves entirely, interior doubled slashes collapse past the scheme delimiter. The shared-tail rule is load-bearing ('/messages' must not eat half of a '/v1/messages' compound); the ExactlyOnce resolver pins are superseded to LosesThemAll at their sites.
  - **The credential gate reads raw_request_authentication() directly** (glm28-6, supersedes glm18-14's delegation shape): SafeGenerationBoundary declares the raw hook (the name SpeaksAnthropicMessagesProtocol already standardizes); the zai surface's vendor getter IS raw (no wrap). The GLM3 #9 wiring contract is unchanged and stated once.
  - **The hidden-include resolution is TWO levels deep, deliberately** (glm28-15): wp_connectors_assignment_value_reasons() owns the per-assignment body once and re-enters only from depth 0. The cap is the complete cycle guard by construction ($a=$b;$b=$a terminates at depth 1 through the fall-through proofs), and a 3-hop chain keeps the generic not-anchored rejection (pinned) — deepening the resolution is a verdict CHANGE, not a refactor, and must come with new fixtures. Naive recursion was verifier-refuted (memory-fatal on cycles; hop-3 flip). The three reason wordings are preserved verbatim; the similar `depends on %s...` wording inside runtime_segment_reasons() is a DIFFERENT site and must not be unified.
  - **One depth-zero split walk** (glm28-10, the glm20-10 precedent): wp_connectors_depth_zero_spans() serves the group-use comma, runtime-segment dot, and map-literal comma/'=>' splits. The predicate returns the cut's BYTE LENGTH (the two-byte '=>' advances past both bytes); spans slice same-length originals (glm17-15). The glm20-10 matcher pin is untouched.
  - **The live smoke twins ride one base** (glm28-11): AbstractZaiSurfaceLiveSmokeTestCase owns the round trip through three owner-class hooks; the glm25-6 lockstep pin is superseded at its site to the new shape (derivation statements pinned once in the base; each twin pins its `::class` hooks — a rename breaks the spelling before any live run).
  - **The usage-rejection wording is reject()'s private detail** (glm28-16): REASON_NOT_OBJECT/REASON_BAD_MEMBER and message_for_reason() are private; failure_reason()/reject() stay public (the glm26-7 lockstep pin and the glm20-6 aggregator source pin pin call-site shapes, not validator visibility — both untouched). The two direct-call suite assertions are superseded behaviorally at their site.
  - **The guidance builder rides Support\BuildsJsonOutputGuidance** (glm28-17, the StashesGenerationPrompt precedent): the trait owns the byte-identical lazy-init block; the per-surface consult (PROVIDER_LABEL, embed/return shape) stays in the models (the glm23-14 per-surface-halves discipline). The glm23-2 memo reflection pin survives the trait flattening unchanged; both suites gained the glm25-3-shape composition pin.
  - **REFUTED this round** (do not re-derive): the trailing-finish_reason type gap — there is NO pre-sentinel asymmetry (the pre-sentinel site stores verbatim too), verbatim storage is the documented pinned design (the streamed-encodability oracle comment names the SDK's downstream is_string gates), and the outcome is byte-identical whether the trailing frame arrived ('stop' gap-fills and succeeds; a non-string or no frame both trip the same vendor gate rewritten to the same generic message; INF takes the stricter typed channel) — GLM6 #2's post-sentinel merging is untouched. The generation_url() alias deletion — the delegator is the entire cost of the glm15-4 cross-surface probe contract (the abstract + uniform probe call deleted the probe's per-surface URL picking); removing it re-introduces exactly that shape plus a facts-table column and three pin supersessions for zero gain.
  - **Micro decisions** (glm28-1/7/8/9/12/13/19/20): the unused-import scanner derives the qualified name from the MASKED match text (glm28-22 hardened the first form: a real-bytes cut at the first comment opener REGRESSED the keyword-to-name comment position — `use /* note */ Dead;` emptied the derived name and silently skipped a dead import the pre-round scanner flagged; on the masked view every comment is a space run the keyword-prefix regex's \s+ spans, and the import's own bytes are never masked, so every comment position derives the clean name — the flag message prints it; the inline-comment INSIDE a qualified name stays lint-owned, a PHP 8 parse error); the duplicate tool_use-id check relies on parse_content_block()'s R9 #3 identity guarantee (a non-null tool_use part carries a non-empty string id); SseFrameBuffer compacts at the entry check only (a drained-but-unpulled buffer briefly holds consumed frames — the docblock's delayed-compaction truth); parse_decoded_message has no `$raw` rebinding; the probe ladder lives on resolve_probe_authentication() (the uncarriable rejection still fires at AUTHENTICATE time inside probe()'s try); the tool_use input judgment is one presence check (absent-or-null, one flag site); the replay-guard bound compare is PHP's numeric-string comparison deciding exactly on both floors (sprintf('%.0f') is the backstop); the encode-rejection template names the JSON_ERROR_DEPTH cause.

- **glm27 round decisions** (fix round over the 3 Codex R21 findings, glm27-1..3; one verifier subagent over the full diff — ALL CLEAR, zero confirmed defects, with the inventories below):
  - **A sound discovery row's every entry must MAP TO METADATA** (glm27-1, NARROWS glm23-7): `is_sound_id_row()` rides the ONE map rule (`id_maps_to_metadata()`: non-empty string AND recognized chat model) instead of the all-string test — `array('')` and `array('unknown-model')` rows are corrupt misses now, never a served empty catalog for the 12h TTL. Verifier-refuted poisoning vectors: every in-repo row writer (both directories' discovery and the probe seed) rides `parse_decoded_chat_ids()`, whose filter calls the SAME `ZaiModelCatalog::is_chat_model()` the soundness rule rides, so no legitimate row is excluded; the unsound flow stays never-fatal including the `_miss`-marker interaction (a live marker serves the fallback with zero discovery attempts, the marker's own contract); the memo cannot serve stale maps through the flip (content-keyed, pure function); glm26-12's flipped static derives from immutable constants. The out-of-band-write class glm23-7 documented is strictly narrowed again. Do not re-flag the recognized-ID requirement as over-validation.
  - **The group-use scanner parses MIXED declarations' typed members** (glm27-2, extends glm20-3): an optional per-member `function `/`const ` kind prefix is stripped before the alias/name parse (the kind never affects the short name; `function helper as h` resolves). Statement-level kinds strip where the prefix is built — no double-strip; uppercase kinds stay skipped with the scanner's whole lowercase-keyword grammar (phpcs's lowercase-keywords sniff owns that class). The 24-shape pre/post differential found no other behavioral delta. Known cosmetic non-defect (pre-existing, diagnostic-only): the group failure line's `qualified` name carries a doubled backslash from the prefix's trailing separator; verdicts ride `short`.
  - **`dependentSchemas` is an object-map schema keyword** (glm27-3, extends GLM8 #6/GLM10 #6): its entries ride `normalize_subschema()` exactly like properties/patternProperties/definitions/$defs; `dependentRequired` is list-valued and correctly NOT in the map; the data-value pass-through (default/examples/const/enum) and the glm10-v2 items/additionalItems tuple rule are untouched.
  - **Verifier-refuted this round** (do not re-derive): legitimate-writer exclusion for the narrowed row rule, stale-map serving through sound↔unsound flips, static-set bleed, double kind-stripping, masked-view interference with the member strip, statement-text mention masking for typed members, and any cross-class interaction from the schema keyword addition.

- **glm26 round decisions** (fix round over the 12 ledger-filtered findings of code-review round 26, high — the round dropped 3 review findings as ledger-covered re-flags (glm21-2's inconclusive-under-distrust, the zai has_more degradation of GLM3 #14-tenant/R15, and GLM1 #15's unknown-block drop; the GLM1 #15 aggregator COMMENT inaccuracy was fixed as glm19-13-class doc drift inside glm26-2); then a two-lens verifier pass — independent security + correctness agents over the full diff, ZERO confirmed behavioral findings, 2 doc-drift items fixed as glm26-13 — with every equivalence claim HOLDING: a 46,656-sequence stop-rule differential (verdicts differ only in the stricter direction, every difference a non-LIFO shape), 642,235 exhaustive plus 200,000 random answer-window histories, a 20,000-id chat-membership fuzz, A→B→A cache-content sequences plus mid-process plan/region retargets, binding-poisoning repros for every wiring shape, key-leakage checks over the new exception channel, and byte-identity differentials over both surfaces' usage rejections):
  - **Uncarriable credential material is a DEFINITIVE INVALID verdict** (glm26-1, extends glm16-13's neighborhood without touching the rejection): the glm16-13 pre-transport rejection rides `UncarriableCredentialException` (a MARKER subclass of the vendor RuntimeException — the FixedMessageResponseException pattern; identical message, family, and ErrorMapper 500 mapping, only `reject_uncarriable_credential()` throws it), and the probe catches it narrowly and answers INVALID. Nothing flew, and the verdict names EXACTLY the credential the probe was about to fly (the wired Api-key, or the effective key the fallback carries — the glm13-1/glm14-5 one-credential-flies-AND-binds discipline holds because the named material IS the rejection); region distrust settles; the generation refusal gate answers before the pre-transport throw; the zai (OpenAI) surface never throws the type (its vendor authenticateRequest flies commas as plain Bearer — unchanged). Do NOT re-flag the no-flight verdict as unproven: the material itself is the rejection evidence, and core's key-save validation refusing the key is the fix's purpose (a pasted trailing newline previously saved as connected with every generation 500ing). Superseded pin: ZaiAnthropicAuthHeadersTest's former configured-pending block, rewritten at its site.
  - **Non-LIFO stop order flags malformed** (glm26-2): the Anthropic wire SERIALIZES block lifecycles, so a stop for N while a lower index is still open is the same corruption class its start-side twins reject (R17 #2 contiguity, R7 #4 duplicate, glm13-5's order) — the stop handler's loop checks every lower contiguity-guaranteed index; the serialized two-block control is pinned alongside the interleave repro. Ascending stops over interleaved starts keep their PRE-EXISTING tolerance (verifier-noted; unchanged by the round). glm26-9's per-block 'stopped' member carries the state the loop reads.
  - **Non-string `delta.role` flags malformed on the zai aggregator** (glm26-3, GLM9 #2 parity): a PRESENT non-string role is corruption (the vendor parse coerces any non-'user' role into a model message); an explicit null keeps the historical skip (isset reads it as absent — the member's absent semantics on this wire).
  - **The zai_anthropic directory's discovery auth-reader rides the raw hook** (glm26-4, glm16-1 alignment): the former wrap reader laundered foreign wiring into the unwired catch inside the recorders — the glm14-5 poisoning held only by call-ordering accident. The FLIGHT keeps the one wrap funnel (line-level unchanged); behavior byte-identical today, the discipline structural. The glm21-14 one-closure pin is superseded to count the raw spelling; a complement pin forbids wrap-getter returns in the file; an opaque-wiring behavioral test covers the directory (fallback serves, nothing flies, nothing records).
  - **The region-pending settle rule is one helper** (glm26-5): `settle_region_pending(bool)` serves the three former hand-copies (isConfigured's two branches, record_definitive_verdict's current-endpoint guard); the phpstan classConstant count drops 13→11 exactly by the two consolidated `static::REGION_PENDING_OPTION` reads (annotated in phpstan.neon.dist).
  - **The discovery-consult skeleton has one owner** (glm26-6, completes GLM4 #10 at the composition layer): `ZaiDiscoveryCache::resolved_map($endpoint_class, $discover, ?$prebuilt)` owns endpoint resolve → cache id → cached_ids → memoized_map; the directories keep only their discovery closures (and, zai-only, the prebuilt seed). The map memo keeps the FILTERED id list and proves unchanged content by a strict list compare instead of re-deriving implode+md5 (still content-keyed under any transient mutation — strictly sounder than the md5 compare, which the verifier showed could collide `"a\nb"` with `["a","b"]`). The option and transient reads stay per-consult BY DESIGN (the glm15-6 boundary: the harness resets transients without hooks). The prebuilt seed is evaluated EAGERLY at the consult, exactly as the pre-round inline form did — the glm26-6 commit's 'lazily' claim was verifier-corrected doc drift (glm26-13); the memo consumes the seed only when it must build, and a stash that outlives its build stays guarded by the glm15-22 mismatch check. Superseded pins: the GLM8 #11 owner-call assertion accepts the orchestrator ride, and the glm15-22 seed pin matches the closure shape.
  - **UsageValidator::reject() owns the usage-rejection throw** (glm26-7): both models ride the one composition (their varying members/lenient/label arguments only); a lockstep pin forbids inline failure_reason/message_for_reason composition in either model.
  - **Directories state their availability pairing through one hook** (glm26-8, the glm24-1 discipline at the directory layer): a protected `availability()` hook per directory (the models' credential_gate_availability() shape, glm14-6) replaces the buried inline instantiations; the lockstep pin ties each hook's product to the registry row's settings class BEHAVIORALLY (settings_class() through reflection) plus exactly-one-instantiation source counts.
  - **The closed-lifecycle state lives on the Anthropic block accumulator** (glm26-9, the GLM10 #7 mirror class): a per-block `'stopped' => false` member (seeded in start_block — the sole block creator, so the member exists on every entry) replaces the `$stopped_indexes` parallel map across all four former sync sites; apply_delta's guard folds into the started-ness it already depended on (stopped ⊆ started by construction).
  - **The answer-window verdicts derive from the outstanding-IDs map** (glm26-10): `$awaiting_answer`, `$opens_tools`, and advance_answer_window()'s by-ref parameter (the file's only by-ref) are gone — a non-empty outstanding map implies an open window (every opener set the mirror; the only clearing boundary requires an empty map or throws), and an empty map at a user-ended boundary was a no-op either way. The GLM5 #19 dead-branch note rides the derived boundary comment.
  - **The zai aggregated() gate reads the choices emptiness alone** (glm26-11, CONSCIOUSLY SUPERSEDES glm19-11's 'load-bearing fields' note — its $event_count half; the GLM10 #4 lesson applied and documented at the rewritten note): choices are written exclusively inside merge_event() (the sole creating writer, verifier-enumerated), which ran only after the deleted counter's increment, so the `0 === $event_count` disjunct was subsumed. The four reflection pins that counted the field are superseded at their sites by the behavioral assertions that implied their information.
  - **is_chat_model() answers from a once-built flipped static set** (glm26-12): the membership RULE is unchanged (verified evidence only); the per-ID loops stop re-merging the three constant catalogs per call.
  - **Verifier-refuted this round** (do not re-derive): cross-credential verdict poisoning for ANY wiring shape (wired/empty-wired/opaque/unwired, both surfaces), key leakage through the new exception or persisted state, stale-map serving under same-cache-id content churn or mid-process retarget, prebuilt-seed poisoning across differing ids, static-set poisoning/bleed, undefined-key reads on the new stop-path guards, false-flag regressions in the stop-rule differential, answer-window divergence for any reachable history, and usage-rejection message/channel divergence between the surfaces.

- **Post-`[DONE]` usage/finish frames are merged** (GLM6 #2, glm6-2): appending-gateway behavior documented in repo records; post-sentinel data completes the payload. Do not flag post-sentinel merging as data-loss.
- **Legacy-zai usage semantics: absent/null members → 0** (GLM7 #8, glm7-8): master parity; the Anthropic-surface strictness is the newer design and does NOT apply to the legacy zai surface. Partially superseded by GLM12 #6 (glm12-6): an absent/null `total_tokens` is now DERIVED prompt+completion (twin parity) instead of defaulting to 0 — the absent→0 tolerance otherwise stands; do not re-flag partial usage objects, only the total member's derivation.
- **Explicit `stop_reason: null` is schema-legal** (GLM8 #4 + GLM10-era streamed twin): Anthropic types stop_reason string|null; accepted, mapped as successful stop. Not missing-data corruption.
- **Strict `is_int` index rejection** (GLM7 #1, pinned): malformed choice/block indexes are typed rejections, not int-coerced. Deliberate hardening vs master.
- **Plan-intersected discovery catalog** (pinned): /models advertising is filtered to the configured plan on both surfaces by design.
- **First-registration-wins provider skip**: registering a second provider with the same ID is silently ignored (registry semantics), documented.
- **Plain unescaped exception messages for constant-only strings** (GLM9-ref): messages built from constant-only strings need no esc_html__; escaping applies to interpolated data.
- **Falsy unsupported-option values: the wire-INERT keys only** (GLM5-era, re-refuted GLM9 — then SUPERSEDED for the forwarded keys by glm4-4/GLM4 #4, commit 81b04d2): setTopK(0), setWebSearch(false), and the output-\* family — keys the request builder never forwards — are neutral no-ops (AdvertisedOptionGuard's `! empty()` gate). The WIRE_FORWARDED keys (presencePenalty, frequencyPenalty, logprobs, topLogprobs) reject ANY explicitly-set value, falsy neutrals included (the guard's `null !== $value` gate; the SDK parent forwards every non-null value verbatim, so setLogprobs(false) shipped `"logprobs":false`). Do not cite the old line to tolerate setLogprobs(false), and do not flag its typed rejection as a regression (glm37-2 scope-corrected this line after round 37's reviewer burned a verifier on it).
- **System-role coercion unreachable via SDK enum** (GLM9-ref): the SDK MessageRole enum cannot express the flagged input; not triggerable.
- **Empty-arguments strings keep null-args semantics** (glm6-15): an empty string arguments field is not corruption; it denotes a no-argument call.
- **`items: []` with additionalItems sibling keeps tuple semantics** (glm10-v2): the empty array is data, not a missing object schema.
- **Bare `event:` wire format absent in production** (GLM1 #14): live z.ai SSE capture (70 frames) contained zero bare event: lines; the handling exists as wire-robustness, not a vendor-verified feature.
- **Whitespace-then-BOM sniff claim** (GLM6-ref): refuted in that round; the GLM8 #2 canonical-prefix fix later made the layers consistent — flag the layers ONLY if a new divergence appears.
- **Binding race in record_definitive_verdict** (GLM8-verifier-ref): not reachable on real WordPress (per-request option pinning); harness-only artifact.
- **Version bump on sub-PR releases** (GLM3-ref): consciously deferred to M7 release tooling, not a review finding.
- **Unknown content-block types dropped → stop-reason consistency documented** (GLM1 #15, commit 2cf214c): vendor-quirk dependent, not triggerable via own requests; documented in code, not fixed.
- **zai-surface `has_more` forever-degradation** (GLM3 #14-tenant, R15 design): conscious design (R15); degradation path documented. Do not re-flag as a bug; propose as an improvement only with new impact evidence.
- **Big-integer replay split rule** (glm12-8): raw-wire paths (zai arguments string, Anthropic accumulated input_json) accept beyond-int literals the platform decode keeps EXACT (`%.0f`-verified) and reject only true precision loss; decoded-only paths (Anthropic non-streaming input, pre-decoded zai members, the consolidated-payload oracle) keep the CONSERVATIVE walker rejection because post-decode exact and lossy big floats are the same double. Verifier-round addendum: the glm12-12 replay stamp does not survive a `toArray()`/`fromArray()` round trip (the vendor DTOs rebuild `new self`), so an exact-literal call that replays in-process rejects conservatively after array rehydration — the rehydrated call is a caller-built decoded-only value, judged by the conservative half of this same rule; not a parse/replay divergence within either surface. Do not flag the conservative rejection as a false positive, and do not flag the precise acceptance as missing validation.
- **Replay-stamp skip** (glm12-12): outbound replay guards skip the serializing oracle for `ReplayValidatedFunctionCall` instances — the args were validated at inbound acceptance and the SDK DTO is immutable (no setters), so the stamp can never outlive its arguments. Caller-built plain SDK instances keep the full oracle. Do not flag the skip as missed outbound validation.
- **Unrecognized 2xx probe bodies stay inconclusive** (glm12-1): only the models-list shape is VALID and only the `success:false`+definitive-code envelope is INVALID; any other 2xx body must neither report connected nor persist an unproven invalid verdict (core clears keys on false). Non-credential envelope codes (1113 balance) stay inconclusive like their 429 twin. Refined by glm13-2: the models-list shape is the ONE entry rule the parser also rejects through (`ZaiModelListParser::entry_failure_reason()`) — an empty `data` list and an incomplete page (`has_more`) are NOT the shape (inconclusive, superseding the verdict's earlier paginated-body tolerance); the chat filter and plan intersection stay catalog-only.
- **Empty wired credential authenticates nothing** (glm13-1): the SDK registry wires `ZAI_API_KEY=''` verbatim, so the probe treats an empty wired `ApiKeyRequestAuthentication` exactly like none (effective-key fallback), and `record_definitive_verdict()` records NOTHING for a request the empty credential flew — the verdict binding must name the credential that flew (an empty-wire 401 under the ladder/DB binding is the cross-credential poisoning class). Do not re-flag the empty-wire skip as missed validation.
- **Generation routes record definitive rejections** (glm13-6): a 401/403 on either surface's generation route records its invalid verdict through the availability base before the typed throw (request-time endpoint capture; glm13-1 binding discipline) — the same evidence the probe and both discovery routes persist. Status-judging parity with GLM10 #8's one-source decision; do not flag the recording as double-state.
- **Unknown delta on a never-started index is corruption** (glm13-4, supersedes the Codex R14 #3 seed tolerance): the synthesized-seed path is GONE — every delta type on an unstarted index rejects (input_json_delta through the tool-input channel, everything else through the malformed-event channel); unknown deltas on a STARTED block keep their forward-compatible tolerance. Position rejections (duplicate/contiguity, glm13-5) run before content validation in `start_block()`, so a position-rejected frame never latches the tool-input flag.
- **The tool-result response encodability guard stays PRE-mapping** (glm13-11 exception): the SDK parent's message mapping json_encodes the tool-result response and STRING-CASTS the failure, so by the createRequest() chokepoint an unencodable result has already laundered into `"content": "false"` — the whole-payload encodability net can never see it. Its guard stays eager in the typed walk while every other pure-encodability member rides the net with attribution-on-failure. Do not "complete" the restructure by moving it. glm14 extensions, both settled: (glm14-3) the vendor parent has a SECOND pre-encode launder site (outbound tool-call arguments, covered by the replay-guard override) and a conditional DROP (response_format under a non-JSON mime) — the net's documented contract is verbatim-forwarded members only; (glm14-1) the dropped-schema case got its own eager guard scoped to the non-JSON mime, so the schema is checked exactly once under every configuration — do not flag the eager check as a redundant second serialization (under the JSON mime it does not run).
- **glm14 round decisions**:
  - **The net's encode rides as the request body** (glm14-4): guard_assembled_params() returns the encoded string when the request carries a JSON body and createRequest() hands it to the Request as raw `$data` — `getBody()` returns it as-is, `getData()` is null on the ridden request, and the vendor transport no longer re-encodes. Byte-identical wire; do not flag `getData() === null` as lost payload data, and do not re-add a second serialization "for safety".
  - **Both mislabeled-JSON fallbacks pass the marker subclass** (glm14-2): `FixedMessageResponseException` propagates through both surfaces' `json_fallback_result()` (null-out catches), and the zai_anthropic surface's tool_use input rejections (missing input member, non-object input value, non-replayable arguments) are marker exceptions now — byte-identical ResponseExceptions distinguished only by type. Do not flag the marker family on the twin as an inconsistency with GLM12 #3/GLM8 #5, and do not flag the pass-through as violating the "header promised a stream" contract (every non-marker parse failure still returns null and keeps the stream-typed error).
  - **Opaque wiring records nothing** (glm14-5, extends glm13-1): a non-Api-key wired `RequestAuthentication` (third-party `setRequestAuthentication()` callers only — the SDK registry type-gates) makes the probe inconclusive without flying and the recorders record nothing; one credential flies AND binds, or no verdict persists. Do not re-flag the opaque refusal as missed validation or as a regression of configured-pending reporting. glm16-1 made the guard REACHABLE on the zai_anthropic surface (the probe reads the RAW wired instance through the `raw_request_authentication()` base hook — the protocol wrap's foreign-auth RuntimeException previously laundered into the unwired catch, flying the fallback key and persisting its verdict); the flight credential still goes through the surface's one wrap funnel.
  - **The generation-route recorder lives on SafeGenerationBoundary** (glm14-6): one concrete `record_generation_route_rejection()` riding the trait's `credential_gate_availability()`/`gate_authentication()` hooks, satisfying ThrowsSafeHttpErrors' abstract; the endpoint stash rides `capture_generation_endpoint()`. Do not flag the hoist as a trait-abstract conflict or as a behavior change (both surfaces' glm13-6 pins drive it).
  - **The parser's reason→rejection map is the single source** (glm14-7): typed ENTRY_* constants, one ENTRY_REJECTION_MESSAGES map, unmapped reasons throw the lockstep RuntimeException. Do not flag the RuntimeException as a new failure mode for data conditions (only an unmapped constant reaches it).
  - **fixed() delegates to the SDK factory** (glm14-8): the marker's message is `ResponseException::fromInvalidData(...)->getMessage()` — a deliberate double construction on rejection paths only. Do not flag the extra allocation as a hot-path cost.
  - **The uninstall constant-rung and one-decode tests run behaviorally, the constant ISOLATED in a subprocess** (glm14-9/10, glm15-1 supersession): ZaiUninstallTest's constant test defines ZAI_API_KEY in a CHILD PROCESS only (the GLM8 #15 runner pattern) — the glm14 "runs LAST" isolation was a file-ordering accident that --order-by=random broke (12F+2E, reproduced), so the ordering property is GONE by design: the check pipeline's test step runs --order-by=random, and the parent process pins it never defined the constant. The probe's one-decode pin counts getBody() reads through a Response double. Do not flag the define() as test pollution, the subprocess runner as harness bypass, or the randomized pipeline order as instability.
- **Both surfaces resolve the transporter before the guards** (glm13-12): the vendor's FINAL `generateTextResult()` calls `getHttpTransporter()` before `prepareGenerateTextParams()` on the zai surface — guards-first is structurally impossible there, so the zai_anthropic surface's own `generateTextResult()` mirrors the vendor-mandated order and identical misuse yields identical errors. Do not re-flag the order divergence in the other direction.
- **glm15 round decisions**:
  - **The zai_anthropic net mirrors the zai surface's, with the recursion exception** (glm15-5, extends glm13-11/glm14-4 to the second surface): ONE raw encode at the zai_anthropic request build proves encodability (attribution walk on failure, mapping-order first-bad-wins) and rides as the body string. The TOOL SCHEMA's encodability guard stays EAGER — the recursive normalize transform would memory-fatal on a self-referential schema no net can reject — and the transform encodes (tool-result content, output-schema guidance) keep their eager forms because their STRINGS ride the wire. Multi-bad payloads now reject on the typed walk before the net (same split the zai surface accepted in glm13-11); single-bad messages are byte-identical. Do not flag the eager schema guard as a redundant second serialization or the body-string ride as lost payload data. glm16-7: both surfaces' nets and walk segments live on ONE shared owner (`Support\EncodabilityNet`); each surface composes the segments in ITS mapping order (zai names the system instruction first on a multi-bad payload, zai_anthropic names text first — pinned both ways), and the sampling-options segment is zai-ONLY because the twin's eager transforms reject those members pre-net. The one-pass source pins scan the OWNER for the single raw oracle now (documented supersession).
  - **The test harness primes discovery before generation-driving auth tests** (glm15-1 second half): ZaiAnthropicProvider::model() consults the vendor parent's STATICALLY cached directory instance; a process-earlier test that wired authentication onto it leaves discovery able to authenticate and consume a queued mock, while an unwired cached instance throws pre-transport (the old accidental pass). Primed transients make model resolution deterministic under any order. Do not flag the priming as hiding the static SDK cache — the cache is vendor behavior the suite isolates around, not a plugin bug.
  - **Memoization boundary: pure derivations only** (glm15-6): the endpoint value instance per class|plan|region and the binding hash per FULL input tuple (class, source, endpoint, key) memoize; every option/env/state read stays per-consult because retarget-the-next-request is pinned. Do not propose per-request state memos with invalidation hooks — the harness resets options without firing hooks, so any such memo is the order-dependence class glm15-1 purged.
  - **The fallback's one decode and the shared non-streaming pipeline** (glm15-7/11, supersedes GLM7 #9's third-hand-off pin count — documented at the pin): both surfaces' json_fallback gates on the shared decoder's raw view (the object-root oracle), and the zai surface's non-streamed and fallback branches ride one parse_decoded_chat_body(). Do not flag the pair hand-off as redundant decodes.
  - **The trait IS speaking the protocol** (glm15-8): SpeaksAnthropicMessagesProtocol carries the auth wrap (raw-authentication hook per class) and the fallback wrap; the directory's insteadof resolution keeps the SDK trait's raw getter reachable. Do not flag the trait's getRequestAuthentication as a trait-abstract conflict or the hook indirection as drift surface.
  - **The discovery build has one rule** (glm15-22): the parse-side build seeds the map memo through the SAME chat filter (map_from_ids' rule); the newest-first ORDER rides the stash's own sort (superseded by glm21-13: the seed applies the canonical comparator itself — pinned at the seed pin). Do not flag the seed as a second build or the stash as state that can go stale (a non-matching stash falls back to the rebuild, pinned).
  - **Each surface slug has one owner** (glm15-23): the SDK-FREE settings layer's CACHE_SCOPE owns 'zai'/'zai_anthropic'; the SDK-dependent PROVIDER_ID and REFUSAL_LABEL alias it (the settings layer must stay SDK-free loadable — Codex R2 #3 — so the alias direction is fixed). Do not propose aliasing CACHE_SCOPE from the provider.
  - **The self-containment analyzer is token-masked, length-preserving** (glm15-2): code-shape scans match on wp_connectors_mask_string_contents()'s same-length copy and slice the real statement from the code view. The masker state machine (heredoc, interpolated strings, curly depth) is pinned by the string/heredoc fixture test. Do not flag the double view as overhead or the token-based strip (comments→spaces, EXCEPT the terminator byte since glm17-14) as an offset hazard — offsets are preserved BY DESIGN.
- **glm16 round decisions**:
  - **A WELL-FORMED duplicate trailing terminal is inert** (glm16-2, corrected by the glm16-15 verifier round, boundary narrowed glm17-6): a duplicate `message_stop` carrying a well-formed payload after a completed generation is a no-op — well-formed means a decodable OBJECT whose type member is ABSENT or agrees with the declaration; an object whose type member is non-string or contradicts the event field (`data: {"type":false}`, `data: {"type":"ping"}`) still rejects through the same termination-independent declaration checks its pre-termination twin faces, so object-ness alone is not the boundary. The appending-gateway tolerance is the OpenAI twin's own sentinel rule, extended to the Anthropic terminal. Content-bearing trailing events (message_start, content_block_*, message_delta) still reject, and a GARBLED duplicate (decodable non-object payload, `data: []`/`["lost"]`) rejects through consume_frame()'s non-object check, which judges the terminal name post-termination too (glm16-15). Do not flag the well-formed no-op as lost corruption detection, and do not re-widen the no-op to payload-agnostic — the initial glm16-2 form did exactly that and silently lost corruption detection for the one declared trailing name whose pre-termination twin rejects the same shape (empirically confirmed by two verifier angles).
  - **enum is a data-valued schema keyword** (glm16-3, extends GLM8 #6): default/examples/const/enum pass through the empty-object normalization walk verbatim — enum members are the CONSTANTS a value must equal, not schema positions. Do not flag the exemption.
  - **The scalar tool_result JSON-encode convention is deliberate** (glm16-4): every response value ships as its JSON encoding, strings included. A raw string would be protocol-legal but the zai twin's tool-message content is the VENDOR parent's own json_encode (unoverridable), so raw here would present the same tool result differently across surfaces. Pinned; do not flag the artifact quotes as a bug.
  - **The tool-schema memo is identity-keyed and declaration-set-scoped** (glm16-6, completed by the glm16-16 verifier round): SplObjectStorage keyed by declaration identity — identity-keyed like the PHP 8.0+ WeakMap it substitutes for on the 7.4 floor, but STRONG-keyed: the storage itself pins every declaration it holds until a reset releases it, the exact pinning the resets narrated here compensate for (glm17-7 wording fix: the old "WeakMap's semantics" gloss denied that pinning in the same sentence that documented it). Reset on config identity change, strict declaration-list change, OR an empty/absent-list build (the vendor ModelConfig is MUTABLE — a public setFunctionDeclarations() — so an identity-only reset let a same-config batch loop pin every superseded declaration, empirically confirmed by two verifier angles; glm17-1: every reset site also sat behind the params build's NON-empty gate, so the clear-to-empty idiom reset nothing and the cleared window pinned the superseded set — an empty/absent-list build releases the memo, and the at-most-the-current-set bound holds at every request build); rejections never memoize. Do not propose a spl_object_id map (GC id reuse), a real WeakMap swap on the 8+ floor (different lifetime semantics than the resets are tuned against), or an unbounded per-instance memo.
  - **is_object_shape() does not encode** (glm16-9): it rides JsonShape::is_list() — key inspection decides identically to the old re-encode probe on every DECODED input, and no encoder means the GLM7 #10 raw-oracle divergence cannot arise; the GLM7 #10 mechanism pin superseded accordingly. Do not flag the missing raw-oracle comment/encode as a lost guard.
  - **Uncarriable credential material rejects typed** (glm16-13): control characters or commas in a NON-EMPTY key throw the RuntimeException binding-failure family pre-transport (HeadersCollection splits comma values; nothing pre-PSR-7 rejected CRLF); the empty key keeps its glm13-1 semantics. Do not flag the rejection as over-validation of admin-supplied keys — the message is fixed and key-free.
  - **The mislabeled-body fallback scaffold is shared** (glm16-8): `Support\JsonFallbackResult` owns the one decode + object-root gate + glm14-2 marker contract for both surfaces; the zai surface's parse keeps its Response argument. Do not flag the shared helper as a new abstraction layer.
  - **The uninstall surface set rides one registry** (glm16-14): the three class-based sweeps iterate `$zai_connector_surfaces`; the class-free option literals and the broken-install fallback literals stay separate BY DESIGN (they must run with no plugin class loaded). Do not propose folding them into the registry.
- **glm17 round decisions** (fix round over the 13 CONFIRMED findings of code-review round 17, then a 6-lens verifier pass — 11 raw candidates, all confirmed on adjudication, 8 fixed as glm17-14..17, 3 documented below):
  - **The tool-schema memo bound holds at every request build under all three reconfiguration idioms** (glm17-1, the round's one code bug): the compare-based resets (glm16-16) sat behind prepareGenerateTextParams()'s non-empty gate, so setFunctionDeclarations([]) reset NOTHING and the cleared window pinned the superseded set while the config held none. A build with a cleared/absent list releases the memo (release_tool_schema_memo()); the bound is a BUILD-TIME bound — between a mutation and the next build the old set stays pinned because the vendor ModelConfig has no observation hook. Do not re-flag the lazy window, and do not propose eager invalidation hooks (the glm15-6 memoization-boundary decision: the harness resets options without firing hooks).
  - **The unused-import scanner's contract is pinned by fixtures** (glm17-8/9/11/12, tests/UnusedImportScannerTest.php): imports are FOUND on the token-masked view (a column-0 use line inside nowdoc/heredoc/block-comment text is DATA), the mention check is case-insensitive over the RAW source minus exactly the matched statement bytes (any mention counts — prose comments, docblocks, a comment carrying the statement text itself), and glm16-17's CRLF/EOF detection delta is intentional. strip_comments PRESERVES the comment token's line terminator (glm17-14: PHP < 8.0 includes the trailing newline in T_COMMENT, and blanking it un-anchored ^use/m on the composer-pinned 7.4 floor — empirically confirmed in docker php:7.4-cli). The view invariant is LENGTH, not text (a legal mid-statement comment blanks to spaces; glm17-15). Do not re-widen the mention check to code-only, and do not move the scanner off the shared token views.
  - **Requiring bin/check-conventions.php is side-effect-free** (glm17-16): the CLI diagnostics (error_reporting/ini_set) run inside the runner guard only; the unreadable-FILE branch counts a violation loudly (@file_get_contents with the false return owned), and an unreadable SUBDIRECTORY aborts with UnexpectedValueException which the CLI gate converts to a counted FAIL (the iterator cannot step past it). Both pinned: a dangling symlink (the one unreadable shape that fails under root too) and the symlink-to-directory skip (plain directories are never yielded by LEAVES_ONLY — the glm17-12 plain-dir fixture was vacuous, verifier-confirmed).
- **glm18 round decisions** (fix round over the 14 CONFIRMED findings of code-review round 18, then a 6-lens verifier pass — 41 raw candidates, 5 CONFIRMED on adjudication and fixed as glm18-15..19):
  - **The conservative walker's out-of-range bound is the 2^63 FLOAT literal** (glm18-1): comparing \abs($value) > PHP_INT_MAX widens the int constant to the 2^63 double and TIES at the boundary magnitude, so every literal of the ~2048-wide collapse window above PHP_INT_MAX (…809 → the …808 double) passed the decoded-only walker and was stamped replayable — the precision-loss poisoning glm12-8's conservative half exists to refuse, with one surface's two delivery channels giving opposite verdicts. The bound is 9223372036854775808.0 now; the exact 2^63 literal accepts ONLY through the raw-wire precise rule. glm12-8 is unchanged and enforced at the boundary — do not re-flag the exact-literal decoded rejection or the wire-path acceptance.
  - **zai inbound tool-call identity members reject at parse time** (glm18-2): the SDK parent coerces a missing/non-string id or name to NULL ('' passes verbatim), and such a turn is the one the outbound replay guard rejects on EVERY later request — the Codex R9 #3 discipline extended to the zai surface's parse hook. Do not flag the rejection as over-validation.
  - **maxTokens positivity rejects typed on the zai surface** (glm18-3): parity with the twin's validate_request guard; the vendor ModelConfig's own schema declares minimum 1. Do not flag 0/-x rejection as a false positive.
  - **The zai_anthropic directory records the 200-carried envelope through the probe's ONE precedence** (glm18-4 + glm18-15): record_rejection_body_verdict() rides the glm12-1 rule INCLUDING its models-list-first ordering — a HYBRID body (a well-formed models list that also carries success:false + a definitive code) is VALID evidence and never records invalid at the directory (the glm18-4 form answered the same body INVALID at one site and VALID at the other, empirically reproduced). Non-credential codes (1113) stay inconclusive. The zai (OpenAI) directory keeps STATUS-ONLY recording — the envelope shape is live-attested for the Anthropic route; do not re-flag the twin's omission.
  - **The debug log viewer's malformed-entry guard is five SCALAR members** (glm18-5 + glm18-16): isset() alone passes array-valued members and the renderer's (string) casts warn — the fatal class the guard exists to prevent. Malformed entries skip; the well-formed rest renders; an all-malformed list renders no table.
  - **List-root output schemas reject on zai_anthropic** (glm18-6): the glm13-8 rule at parity — JsonShape::is_list() before the encodability guard, on either signal; [] rejects with it.
  - **The self-containment scanner's write-visibility contract** (glm18-7/8 + glm18-17/18/19; pinned by tests/SelfContainmentLoopWritesTest.php and SelfContainmentCompoundWritesTest.php): an include at $offset reads writes from [0, $offset) PLUS every loop construct containing it (do-while spans include the trailing condition; braceless do over-approximates to EOF) PLUS every function-declaration body containing it (re-entrancy: recursion and repeated callback invocation execute later writes before the include's next activation). Compound assignment operators are writes in BOTH write-shape checks; collecting a compound write with its RHS keeps the every-assignment-must-prove rule covering unions; a by-reference alias whose SOURCE is the include variable refuses the plain-variable proof. Regions are OVER-approximated by design (alternative-syntax bodies, unparsable tails, unclosable braces extend to EOF) — a wider region only refuses proofs, never launders one. Do not flag the EOF over-approximation without a real-tree false violation, and do not re-narrow the visibility model.
  - **Known scanner boundaries** (verifier-demonstrated laundering shapes; phpcs-gated or signature-analysis-bound): by-reference CALLEE parameters (a function taking the include variable by reference writes it with no textual write to match — closing it needs callee signature analysis), `global`-binding cross-scope writes (a function-scoped include reading a top-level write placed after the function definition), and function-PARAMETER bindings on the plain-variable path (the caller's argument wins; the map path refuses signatures, the plain path does not model them). goto backward edges and uppercase-keyword loops launder through the scanner itself but phpcs (Generic.PHP.DiscourageGoto, the lowercase-keywords sniff) fails both shapes, so neither can land while `composer phpcs` gates the tree; hostile unbalanced-brace input degrades the span walks to O(loops × length) but @lint owns that class (the glm17 unbalanced-string sibling).
  - **Refactor decisions** (glm18-10..14): one sanitize_enum() serves the plan/region read AND write paths; the discovery cache-id FORMULA lives once on the endpoint base (compose_discovery_cache_id(), parameterized — LSB stays out of the composition, constant VALUES stay settings-owned per glm15-23, and the broken-install fallback rides it, superseding GLM12 #10's inline mirror); messages_url() routes through api_url(); the availability base's mark_region_switch_pending() delegator is DELETED (zero callers — the SDK-free settings owner is the one entry point); gate_authentication() delegates to raw_request_authentication() so the credential gate and the protocol wrap share one body.

- **glm25 round decisions** (fix round over the 11 findings of code-review round 25, high, ledger-filtered — the 25th round, zero correctness findings again, all cleanup/drift-risk; then a two-lens verifier pass: independent security + correctness agents over the full diff, 1 CONFIRMED finding — the round's own glm25-9 stat-guarded head memo served a STALE head for a same-path, same-size rewrite inside one integer-second mtime (back-to-back repro 30/30; latent only — no current consumer rewrites mid-run), excised as glm25-12 — with every equivalence claim HOLDING: a 22,757-sequence SSE prefix-machine differential plus a 60,000-case fuzz (both zero divergences), a two-tree discovery-seed write differential (identical transient name/value/TTL), a refusal-gate decision matrix including the empty ApiKey key through effective_for_authentication()'s '' branch, byte-compared tokenizer views over 62 real files and 12 hostile fixtures, and mutation-tested pins catching every reintroduced-literal and second-writer shape including heredoc-hidden writers):
  - **The positive discovery row has ONE writer family** (glm25-1): `ZaiDiscoveryCache::store_ids()` is the single positive-row store — `cached_ids()`'s own write routes through it — and the availability base's probe seed calls the owner. A tree-scan pin (ZaiModelDirectoryTest::testThePositiveDiscoveryRowHasOneWriterFamily) holds that every `set_transient(` under connectors/zai/src outside ZaiDiscoveryCache.php is the availability base's ONE probe-miss marker write. Do not re-add a second writer of the row (reader-side contract: glm23-7).
  - **The refusal gate and message builder are PRIVATE** (glm25-2): `generation_refusal_reason()` takes the always-narrowed `ApiKeyRequestAuthentication` as a REQUIRED parameter behind `generation_refusal_for_wired_authentication()`; `refusal_message()`'s only caller is `refuse_generation()`. The fixed wording is pinned per surface through the refuse_generation() THROW (zai invalid-verdict in its suite, zai_anthropic region-pending in its) — not cross-class static builder calls — plus a pin that foreign wiring skips the THROWING wrapper. Do not re-widen the public surface or re-add a default-null credential path.
  - **The generation_prompt stash rides Support\StashesGenerationPrompt** (glm25-3, the MemoizesToolLoopVerdicts precedent for the parents-differ shape): both models compose the trait; the assignment stays at each surface's prepareGenerateTextParams(); the failure closure reads `stashed_generation_prompt()`. Both suites pin the composition, the absent hand-synced declaration, and the assignment statement. Do not copy the property back into a model.
  - **The Messages non-streaming decode is ONE inline at the sniff else-branch** (glm25-4): the parse_message_body()/parse_body_string() two-hop chain is gone (the zai twin's shape); the glm15-7 one-decode pin's per-surface `JsonBodyDecoder::decode( $body )` substring is SUPERSEDED at its site — it named the deleted wrapper's parameter spelling — and now states each surface's own decode site plus the shared `JsonFallbackResult::parse(` funnel both fallbacks ride.
  - **feed()'s no-BOM branch owns no state transition** (glm25-5): the dead PREFIX_SETTLED assignment is deleted; the unconditional settle at the prefix block's end owns it (the AFTER_BOM branch is the only path that may return still-undecided). Verified dead by two independent differentials (22,757 sequences + 60,000 fuzz cases). Do not re-add a "defensive" per-branch settle.
  - **The live-smoke lockstep pin covers BOTH twins** (glm25-6): the zai smoke test rides PlanRegionSettings::OPTION_PLAN/OPTION_REGION + DEFAULT_PLAN, AbstractPlanRegionSettings::DEFAULT_REGION, and ZaiProvider::PROVIDER_ID; ZaiSurfaceLockstepTest loops both live-smoke files with per-surface owners. Extends glm21-15's pin to the second surface.
  - **The conventions gate tokenizes each file once** (glm25-8): `wp_connectors_file_code_views()` serves both the self-containment driver and the unused-import scan the (source, code, masked) triple, memoized per file CONTENT (path + md5 key) — sound under rewrite BY DESIGN, no mtime granularity; the unreadable return stays caller-owned (the scan's loud FAIL, the driver's empty-analysis tolerance, which equals the old (string)-cast semantics since strip('')/mask('') are both ''). Do not add stat/mtime-keyed memos here (see glm25-12), and do not move a check back to its own strip+mask pair.
  - **version_constant_violations() takes the caller's $mainFiles** (glm25-9, the main_file_violations() idiom — rescan-when-empty fallback; all three CLIs pass the list) **and the 8 KB head read is deliberately UNMEMOIZED** (glm25-12): the stat-guarded head memo this round first added (mtime + size) served a stale head for a same-path, same-size rewrite inside one mtime second — verifier-reproduced 30/30 — while saving only page-cached reads; `wp_connectors_plugin_file_head()` is a plain read now, correct by construction. Do not re-add a stat-guarded head memo; a content-keyed one is pointless for heads (reading to hash is the read).
  - **glm25-10**: every Anthropic /models success body in the metadata/availability and auth-headers suites rides HttpResponseFactory::anthropicModelsBody() (the glm15-20 drift class closed on this surface; the factory body's extra members — has_more:false, first_id/last_id, created_at, display_name — change no verdict: entry_failure_reason() reads only data[].id and the exactly-false has_more, glm13-2/glm23-7 safe in both directions, verifier-differential).
  - **glm25-11**: `WpConnectorsTestCase::declared_constants()` (the aggregator_state() precedent) owns the declaring-class reflection walk; all five former copies (EndpointResolver ×2, Settings ×2, the deleted private helper) ride it.
  - **Verifier-refuted this round** (do not re-derive): seed-write what/where/whose/TTL divergence (two-tree differential byte-identical), refusal-gate decision changes for any wrapper-passable input, view-memo staleness/symlink-aliasing/unreadable-swallowing classes on the gate, scanner gate vacuity (identical-content two-path, rewrite races), pin vacuity on the rewired tests (mutation-tested: every reintroduced literal and second-writer shape caught), factory-body verdict changes, SseFrameBuffer behavior change (82k+ total fuzz cases, zero divergence), and version-check threading changing the no-main-file error path (byte-identical both call forms).

- **glm24 round decisions** (fix round over the 9 findings of code-review round 24, high, ledger-filtered — zero correctness findings, all drift-risk/maintenance; then a two-lens verifier pass: independent security + correctness agents over the full diff, 1 CONFIRMED finding — the round's own new slug pin was vacuous against the verbatim revert it existed to catch, empirically scored against the pre-round source, fixed as glm24-10 — with every equivalence claim HOLDING: a 200k-sequence fuzz on the has_json derivation, a 13-shape truth table plus gated battery on the content probe, byte-identical scanner violation lists on hostile fixtures and the real tree, and a 5-shape ladder battery):
  - **The live probe states no availability class** (glm24-1): the facts table's 'availability' column is GONE — its only consumers read KEY_OPTION/STATE_OPTION, constants the availability layer aliases from the settings class the registry row already carries (glm15-23's fixed alias direction), so the probe reads both option names through `$surface_facts['settings']::`. ZaiSurfaceLockstepTest pins the two derivation statements and forbids every word-bounded availability-class restatement (names derived per provider through its own availability() factory); the per-fact table pin in ZaiLiveProbeArgsTest drops the column. Do not re-add the column or a per-surface availability pairing anywhere the registry row already names the settings class.
  - **Both card names alias the settings layer's PROVIDER_LABEL** (glm24-2): ZaiAnthropicProvider::PROVIDER_NAME is a constant-expression alias of ZaiAnthropicPlanRegionSettings::PROVIDER_LABEL, and the zai twin's provider_display_name() returns PlanRegionSettings::PROVIDER_LABEL — the PROVIDER_ID/CACHE_SCOPE pattern. The lockstep pin reads the card name through provider_metadata_args()[1] on BOTH surfaces (the pre-existing per-side literal pins keep the values non-vacuous). Do not re-inline either literal.
  - **The auth rejection messages ride the label chain — the glm19-5/6 deferral is CLOSED** (glm24-3 + glm24-10): ZaiAnthropicRequestAuthentication's two RuntimeException messages (uncarriable credential, wrap() type refusal) interpolate a private PROVIDER_LABEL = ZaiAnthropicProviderAvailability::REFUSAL_LABEL; output byte-identical. The glm24-10 pin names the verbatim-revert shape on BOTH halves — the positive sprintf-idiom count (a revert scores 0) plus the mid-string sentence-prefix and standalone-slug prohibitions — and both behavioral pins ride REFUSAL_LABEL-derived expectations (the wrap() pin is the FULL message). Do not flag the sprintf indirection as diagnostic laundering, and do not weaken the pin to a standalone-slug-only match (the verifier-reproduced vacuity).
  - **has_json derives from the accumulated JSON** (glm24-4): `'' !== $block['json']` at the single read site; the stored mirror and its append-side guard are gone. The equivalence rests on the closed write set (an '' init plus ONE string-gated append — a null fragment cannot reach the append), verifier-fuzzed over 200k fragment sequences with zero divergence. The GLM10 #7 hand-synced-mirror drift class; do not re-add a stored presence flag.
  - **start_block()'s null-raw_block conjuncts were dead** (glm24-5): $type derives solely from $raw_block and the null-type early return settles null-ness, so the tool_use branch and the four accumulator member reads dropped their `null !== $raw_block` conjuncts (even counterfactually the isset() reads would evaluate identically on null). The null-type branch is the ONE live null handler; do not re-add "defensive" null checks past it.
  - **The self-containment analysis rides ONE masked view per file** (glm24-6): the driver computes the string-masked view once and threads it down — array_writes_recognized() takes the view directly (its $code param existed only to mask), and the assignment collector, runtime-segment, hidden-include, and array-literal helpers carry ($code, $masked) pairs so offsets slice real statements. Measured 63 mask passes / 861 KB → 55 / 742 KB per connectors/zai scan (every remaining pass a distinct file's driver pass); violation lists byte-identical on hostile laundering fixtures and the real tree, both directions. Do not re-add callee-side masking "for safety".
  - **The decoded-message content probe states its order** (glm24-7): property_exists FIRST (truth-table-identical over a 13-shape battery; the former `?? null` existed only to keep the inverted order safe), and no $raw_content_ok mirror — a non-null $raw IMPLIES a present-array content member past the entry gates (the associative gate proved the member exists in the same payload's other decode; the object-ness probe proved it is an array; both body callers derive the pair from ONE body through JsonBodyDecoder). Do not re-introduce the mirror.
  - **effective_key() extracts the first rung directly** (glm24-8): array_key_first() over the owner-ordered non-empty ladder; an empty ladder falls to the database branch exactly as before (5-shape battery). key_source()'s genuine search loop is the contrast case; do not re-add the loop silhouette.
  - **uninstall.php collects the ladder with array_values()** (glm24-9): the source labels were never read downstream (array_unique + foreach-by-value only); glm13-15's one-occurrence pin on ::env_constant_ladder() holds.
  - **Verifier-refuted this round** (do not re-derive): wrong-surface option writes via the probe derivation (all four option names derive from the ONE registry row's settings class), key leakage or error-channel change in the folded messages (live throws byte-identical and key-free), cross-file/stale-view contamination in the threaded analyzer (single-source call graph, pure masker, all 14 call sites audited), and the obfuscated-slug bypass classes (concatenation/heredoc/hex spellings — subsumed by the glm24-10 pin naming the revert shape directly).

- **glm23 round decisions** (fix round over the 15 findings of code-review round 23, high, ledger-filtered; then a two-lens verifier pass — independent security + correctness agents over the full diff; ZERO confirmed findings — the security lens refuted 12 candidate classes with empirical repros, the correctness lens drove a 30-case SSE battery, a 19-shape joined-vs-split differential, and a WP 6.8 core-source comparison; every fix held):
  - **A data-less content-block declaration is a CUT frame with a ONE-FRAME completion window** (glm23-1, narrows GLM8 #1's data-less rationale): the GLM8 #1 ignorable classes are now the LIFECYCLE events (message_start/message_delta/message_stop — absence-guard covered) and unknown names ONLY; a data-less content_block_start/delta/stop declaration opens $pending_content_declaration, settled by the NEXT frame (a data-only carrier REUNITES the pair — the carrier is judged as if the declaration rode its own frame, so every downstream corruption rule covers the split form; anything else flags) or by the finish() override at EOF, always through the one flag_corrupt_event() site (the GLM12 #15 source pin counts SIX sites now). Do not re-widen to an immediate flag (it broke the suite's split-frame fixtures — the GLM1 #14 bare-event tolerance), and do not re-narrow to ignorable (the verifier-reproduced silent-truncation hole). The window's tolerances are pinned: the GLM10 #5 bare-[DONE] skip, glm16-2's duplicate-terminal no-op, and the data-less lifecycle/unknown tolerances all survive (verifier-empirical); a REUNITED [DONE] carrier judges malformed exactly like its one-frame twin, which GLM10 #5 itself mandates.
  - **The dropped outputSchema embeds guidance on zai; one JsonOutputGuidance owner serves both surfaces** (glm23-2): a configured schema under a NON-JSON mime — the vendor parent drops response_format outside application/json, and the Codex R1 #4 guidance remedy existed on the zai_anthropic twin only — embeds the byte-identical guidance into the system instruction via the zai surface's prepareMessagesParam() override, scoped exactly to the dropped case the glm14-1 guard scopes. The vendor ModelConfig's setOutputSchema() AUTO-PROMOTES a null mime to application/json (so a schema with an UNSET mime always takes the native response_format path — the round-23 finding's leave-it-unset repro is unconstructible through the public setters); the constructible dropped case names a non-JSON mime explicitly. The guidance sentences and the glm21-7/16 memoized schema encode ride Support\JsonOutputGuidance (per-model instance, the memo pin reads the builder's encode_memo through the reflection helper); under the JSON mime NO guidance is added (the vendor-native response_format.json_schema constrains). Cross-surface byte-identity is pinned.
  - **register_argc_argv=0 walks the usage path** (glm23-3): the live probe's argv pre-scan and getopt() both normalize the unpopulated-argv shape (empirically unset in docker php:7.4-cli/8.3-cli; PHP builds newer than the composer platform ceiling ALWAYS populate argv in the CLI SAPI, where the pin asserts the same outcome vacuously). The two guard reads ride count-pinned phpstan ignores (phpstan's argv type reflects the always-populated modern builds).
  - **glm23-4/5**: the enum renderer resolves label methods on the CHILD (static:: late binding; pinned with an overriding-child fixture), and DEFAULT_PLAN is child-owned like every other per-surface identifier (the base declares none; the reflection pin covers it; the phpstan classConstant count for the settings base is 31 now).
  - **The undecodable zai data frame flags in both phases** (glm23-6, restores the docblock claim glm19-11's counter deletion stranded): json_last_error() distinguishes the UNDECODABLE shape (flagged, pre- and post-sentinel — the flag half of GLM7 #2's "malformed ones still counted") from decodable non-array payloads (a scalar `data: null` keeps its non-event skip, the same tolerance merge_event() gives an object without choices). An EMPTY `data:` value flags too (an empty data line is a cut payload under the same rule — deliberate, no live-capture evidence of a benign empty data frame). Two GLM7-era assertions that encoded the silent skip are superseded at their sites. The sentinel check still precedes the decode (duplicate [DONE] never flags), and the GLM8 #5 mislabeled-JSON fallback is untouched (a mislabeled body produces zero data: field lines).
  - **A sound discovery row is a NON-EMPTY all-string list** (glm23-7; the string test NARROWED by glm27-1 to require recognized chat-model IDs — see the glm27 section): anything else under the 12h transient is a cache MISS (the negative-marker/plan-fallback paths run exactly as for an absent row); both surfaces' discovery throws on an empty post-intersection list (glm13-2), so no legitimate writer can cache one. An attacker-written sound-shaped row still serves verbatim — the pre-existing out-of-band-transient-write class, strictly narrowed.
  - **The update_option stub matches core** (glm23-8/9, harness-only): the unchanged-value short-circuit runs BEFORE any autoload handling regardless of the autoload argument (return false, no write, no hooks), and the hook order is generic 'update_option' (3 args, pre-write) → write → specific update_option_{$option} (3 args: old, value, option) → 'updated_option' (3 args) — verified against WP 6.8 wp-includes/option.php. Pinned with order/arity/pre-write-recording assertions.
  - **glm23-10..12 harness consolidations**: ONE FakeSecrets key serves the boot's stored row and wired credential (pinned stored === flying in both mapping suites); wiredZaiSurfaceModel() is the class-parameterized model wiring (the surface row — and with it the primed endpoint — derives from ZaiSurfaces by PROVIDER_ID, the lockstep pin's identity); bootedCorePromptBuilder(provider_class, models_body) derives every surface fact the same way (the settings KEY_OPTION stores the row — the availability classes' SDK-free mirror, pinned equal), the one protocol-specific fact is the caller's parameter, and a third surface boots with no harness edit.
  - **The uninstall class-free literals ride a lockstep source pin** (glm23-13): the class-free deletions and the broken-install probe-prefix fallback stay hand-enumerated BY DESIGN (no plugin class may load there) and are now pinned against ZaiSurfaces + the settings owners' constants and probe_miss_transient_prefix() — a rename breaks the pin before any uninstall strands options.
  - **The tool-loop memo trio rides Support\MemoizesToolLoopVerdicts** (glm23-14): both models compose the trait (the parents differ; the trait-flattened properties keep the reflection pins working unchanged). The PER-SURFACE encode halves stay in the models BY DESIGN — zai's prove_tool_result_encodable() stores a TRUE verdict (the eager glm13-11 exception), the twin's encoded_tool_result_response() stores the ENCODED STRING (glm16-4) — reading the trait's shared store; do not "unify" them (different guard semantics), and do not copy the trio back into a model.
  - **glm23-15**: reject_out_of_unit_interval() takes the range phrase as the caller's parameter (the PROVIDER_LABEL pattern) — the [0, 1] bound and its naming are Messages-protocol facts the protocol-neutral shared guard no longer hardcodes; wire message byte-identical.
  - **Verifier-refuted this round** (do not re-derive): guidance-embed injection (rides as a JSON string value; outputSchema has zero production writers — caller-config trust, byte-identical to the 22-round-old twin embed), guidance stacking/memo staleness on the moved builder, a false-accept or tolerance regression in the completion window (19-shape differential), a json_last_error cross-call leak, cache poisoning via is_sound_id_row, secret leakage in new messages, member-set damage from the trait extraction (property sets compared pre/post), stub divergence from core, and every double/missing-embed shape for the dropped case (one funnel, one prepareMessagesParam call).

- **glm22 round decisions** (fix round over the 15 findings of code-review round 22, high, ledger-filtered; then a two-lens verifier pass — independent security + correctness agents over the full diff; 2 raw candidates, both CONFIRMED on empirical repro and fixed together as glm22-16; the remaining attacks refuted — see the round's refuted list):
  - **The out-of-range walker judges the DECODED WIRE FORM** (glm22-1 + glm22-16, closes the non-stdClass escape the round found and supersedes any property-graph completion of it): has_out_of_range_integer_float() serves is_replayable() by walking the decode of the oracle's own phase-one encoding, NOT the caller's tree — json_encode()'s member set differs per class (public props for a plain object, jsonSerialize()'s OUTPUT for a JsonSerializable, the internal STORAGE for an ArrayObject/ArrayIterator), so a property-graph walk is inexact in BOTH directions (verifier-reproduced: an ArrayObject dynamic property never ships but rejected; its storage ships '{"x":9.3e+18}' but passed; a cyclic JsonSerializable property graph walked to an uncatchable memory-exhaustion fatal while its clean serializer output encoded fine). Decode products and plain arrays/stdClass keep identical verdicts (shortest-roundtrip spellings decode back to the same double). The walker's JsonSerializable + cycle-guard branches are the misuse backstop for a caller handing the walker an object directly (the fast path's contract stays decode products). Do NOT propose property-graph member walks, per-class serialization mirrors, or removing the backstop branches.
  - **'+' joins the plain-integer scan's adjacency guard class** (glm22-2, extends glm19-1/glm20-13): JSON's only '+' is the positive exponent sign, so a digit run preceded by '+' is always a float token's exponent — the plain scan read 0e+…809 as the standalone …809 and falsely rejected an exact zero while its e-/unsigned twins accepted. The e+/e-/unsigned trio is pinned at the rule, the zai inbound channel, and the streamed channel; the verifier's differential battery (pinned corpus + 200k fuzzed tokens) found no false-accept direction. Do not re-widen the acceptance analysis without a new decodable shape.
  - **The zai surface rides the tool-loop memos** (glm22-3/4, the twin's glm21-4/5/17 discipline ported whole): the tool-result encodability VERDICT and the caller-built replay verdict memo by DTO identity on the immutable vendor DTOs, with the build-set sweep (re-armed at prepareGenerateTextParams, noted in the validate walk — exactly the DTOs the parent maps — pruned after the params build; mid-throw never prunes; rejections never memoize; the parent's own shipping encode is structural). The verifier traced every consult strictly before the prune. Do not re-flag the port's discipline as divergent from the twin's mapping-time notes — the walk notes the same set by construction.
  - **Round-22 harness consolidations** (glm22-6..12, byte-identical-behavior extractions in the glm19-5/glm20-11 direction): two harness-owned abstract bases (AbstractZaiSurface{Request,Response}MappingTestCase) carry the protocol-independent mapping members — one copy executes per concrete surface, and the drifted maxTokens pins are UNIFIED (0 AND -5 on both surfaces, superseding the per-suite pins; coverage strictly grew); wiredZaiModel()/wiredZaiSdkInstance()/bootedCorePromptBuilder($slug) own the wiring shapes; primeZaiSurfaceDiscoveryTransient() parameterizes the priming twins (the glm15-12 harness composition pin counts ONE now — documented at the pin); OpaqueAuthentication is the one harness-owned identity-passthrough double. The deliberately-unbound factory calls (testAnUnboundInstanceFailsIdenticallyOnBothSurfaces, the binding-hint test, the custom-transporter directory construction, the unwired cache-key probe) stay inline BY DESIGN.
  - **Micro decisions** (glm22-5/13/14/15): CANONICAL_BASE_URL derives from its MATRIX cell (value-identical, 7.4-legal); strncmp replaces the strpos prefix probes (fuzz-equivalent: 60k×6 local, 3M pairs verifier; the frame-delimiter searches stay strpos — they are searches, not probes); the content_block_start pre-check is GONE (start_block()'s null-type branch flags identically, glm13-5 order untouched — a 29-case old-vs-new matrix found zero divergence); wp_connectors_blank_quoted_strings() owns the array-literal walk's blanking (deliberately NOT the token-aware masker).
  - **Verifier-refuted this round** (do not re-derive): memo staleness on the ports (covered by the glm21 value-graph-freeze boundary — no new production mutation path), strncmp/strpos divergence (3M binary pairs, zero), the pre-check deletion changing any aggregator state (29-case matrix), '+' creating a false accept (differential battery), the harness consolidations leaking into production (no production autoload maps tests/; the secret scanner sweeps the harness clean).

- **glm21 round decisions** (fix round over the 15 findings of code-review round 21, high, ledger-filtered; then a verifier pass — 6 lenses over the full diff, 7 raw candidates, 4 CONFIRMED on adversarial refutation and fixed as glm21-16..19, 3 refuted):
  - **The plain stream-start whitespace run strips on both layers** (glm21-1, supersedes the GLM8 #2-era documented drop and closes the asymmetry the GLM6-ref round left): `strip_stream_prefix()` and feed()'s incremental prefix machine strip the leading LEADING_WHITESPACE run even when no BOM follows, and EventStreamSniff's private ltrim is deleted — what the sniff accepts, the framing parses, byte for byte (the chunk-split alignment pin still proves the composition). Mid-stream whitespace keeps its spec-strict unknown-field meaning (the strip is stream-start only), and the JSON callers (JsonBodyDecoder, ZaiModelListParser::decode_models_body()) extend the same gateway-prefix tolerance to NUL/vtab-prefixed bodies. Do not re-flag the plain-run strip as spec noncompliance, and do not re-add a sniff-side ltrim. The round-21 finding's Anthropic aggregated()===null mechanism does NOT reproduce (a column-0 `data:` line still carries the type member past the lost `event:` declaration); the pinned damage shapes are the OpenAI sentinel loss (post-sentinel frames merging as pre-sentinel content) and the swallowed whitespace-prefixed error declaration.
  - **Region-switch distrust treats a persistently-inconclusive /models as not-configured** (glm21-2, consciously accepted; the R15/SPEC §3.3 design meeting the glm13-2 verdict widening): under REGION_PENDING_OPTION, isConfigured() returns false for an INCONCLUSIVE probe — including the 200-body classes glm13-2 made inconclusive (an empty data list, an incomplete has_more page, a non-JSON 2xx body) that master's status-only probe blessed as connected. Residual cost: a valid env/constant key switched onto a region whose /models persistently answers one of those shapes keeps the connector not-configured and generation refused ('pending revalidation after a region switch'); probes throttle to one per PROBE_MISS_TTL (60s) window; recovery requires the endpoint answering definitively (a real model list or the definitive-rejection envelope) or admin intervention (region back, or a re-saved/re-wired credential — a DIFFERENT key is not the riding key and keeps normal semantics). SPEC §3.3 wins over the empty-2xx master delta: configured-pending under distrust would send the old-region credential against the new endpoint indefinitely. Do not re-flag; reopen only with a live capture of a new-region /models answering an inconclusive 200 shape for a VALID key.
  - **A broken-install uninstall leaves the discovery transients standing** (glm21-3, consciously accepted; the GLM8 #15/glm20-4 degradation boundary): the class-based discovery sweep (every plan × region of both surfaces, '_miss' markers included) runs only when the eight-file owner chain loads — the broken-install fallback literals deliberately cover only the class-free options and the two key-state probe-miss prefixes (they must run with no plugin class loaded). Residue on a broken install at Delete time: the 12h discovery transients (zai_connector_zai_models_&lt;md5&gt;, zai_connector_zai_anthropic_models_&lt;md5&gt;, and their 60s '_miss' twins) survive — in the external object cache on object-cache installs (invisible to any wp_options enumeration), or as wp_options rows nothing deletes on DB installs (the LIKE sweep iterates only the probe prefixes). A reinstall inside the TTL window is served the stale pre-uninstall catalog until the TTL lapses; the master delta is master's unconditional literal sweep of the same ids. Accepted because deriving the ids requires the endpoint classes (the formula's one owner), and mirroring the formula literally in this file is the drift class GLM8 #11/GLM9 #8 removed twice. Do not re-flag; a fix needs a class-free derivation source or a conscious decision to re-accept the literal mirror.
  - **The tool-loop memos are identity-keyed and pruned to the completed build's tool-DTO set** (glm21-4/5, completed by the glm21-17 verifier round): the historical tool-result response ENCODES and the caller-built replay VERDICTS memo by DTO identity on the immutable SDK DTOs (first run full, repeats served; rejections never memoize — the glm16-6 discipline). The release bound is the build-set SWEEP — every tool DTO a request build maps is noted, and prune_tool_loop_memos() detaches every entry the completed build did not map: at most the previous and current build's tool parts at any moment, whatever the conversation shape. The first form (a first-tool-DTO conversation anchor) was verifier-reproduced to miss the rotating-tail shape (one persistent call, rotating responses — every superseded entry pinned; the glm16-6-forbidden unbounded shape). Rehydrated (toArray()/fromArray()) conversations carry fresh instances every request and miss BY DESIGN: any value-key would pay the serialization the memo exists to skip. Tool-less builds allocate nothing and prune nothing; a mid-throw build never prunes (entries are pure, a detach only costs re-derivation). Do not propose value-compare invalidation (the compare IS the memoized cost; glm15-6 forbids invalidation hooks) and do not re-flag the identity keying.
  - **The DTO value-graph-freeze boundary extends glm12-12's stamp boundary** (glm21 verifier round, refutation record): the memos above share the stamp's trust boundary — an in-place mutation of a value OBJECT inside a held DTO's args/response between builds of one conversation serves a stale verdict/encoding. Refuted as a defect because the refuter reproduced the IDENTICAL pre-round failure through the pinned glm12-12 stamped channel (the stamp skips the oracle on the same immutability rationale; the whole-payload net is an ENCODABILITY net and a precision-loss float encodes cleanly, so nothing downstream catches it there either); no production path mutates a held DTO's value graph (ordinary inbound args are arrays with value semantics; the conversation-resume shape creates fresh instances), and glm19-3/14 pins a looser boundary on exactly the caller-built stdClass channel. In-place mutation of already-sent historical values is out-of-contract. Do not re-flag without a production mutation path.
  - **The outputSchema memo serves OBJECT-FREE schema graphs only** (glm21-7, refined by the glm21-16 verifier round): the encoded-schema-string memo's value compare is a STRICT array compare gated by schema_is_strict_comparable() — sound only where the graph carries no objects (arrays are values; every mutation changes the compare), and reading not-comparable for object-carrying, >64-deep, or reference-cyclic graphs, which skip the memo and encode every build (the pre-glm21-7 behavior, correct for every shape). The gate exists because the strict compare judges nested objects by IDENTITY — the verifier reproduced the memo serving a pre-mutation encoding forever after an in-place nested-object mutation. A serialize()-signature compare was tried in the fix round and REJECTED: serialize() fatals on anonymous-class and closure members that json_encode handles fine, trading staleness for a new rejection class. Do not propose signature-based compares, and do not widen the gate to object graphs without a compare that is sound under in-place mutation.
  - **Round-21 refactor decisions** (glm21-6/8/9/10/11/13/14, byte-identical-behavior extractions in the glm19-5 direction): the unit-interval rule (temperature/top_p) and the outbound replay block (stamp skip + oracle + throw, with an optional oracle callable interposing the glm21-5 memo) live on RequestShapeGuard and ToolArgsReplayGuard; the outbound replay wording is UNIFIED on 'could not replay tool call arguments' (both surfaces, byte-identical otherwise); UsageValidator::sum_members() is public and serves the zai absent-total derivation; Support\StreamIndex::sound() owns the stream-index value rule for both aggregators (container fetches stay per-caller); ZaiDiscoveryCache::id_maps_to_metadata() owns the metadata-map filter+key rule and the cold-discovery seed applies the canonical comparator itself (the glm15-22 ORDER clause — "rides the stash's own sort" — is superseded by glm21-13; the supersession is pinned at the seed pin); the zai_anthropic directory's discover_model_ids() uses ONE auth-reader closure and ONE rejection helper for its five sites; bin/zai-live-probe.php builds its surface map from ZaiSurfaces (SDK facts keyed by settings class, CLI whitelist and default derived, missing-facts exit 3) with the lockstep pin upgraded to the full-pairing form (superseding glm20-4's substring pins, documented at the pin). Pin relocations from the glm21-18/19 verifier round: the live-smoke source pin lives in ZaiSurfaceLockstepTest (the smoke suite's setUp skips everything without the live key), and the debug-field pin carries its source half (no hardcoded pair; the derivation statement present) — the behavioral half alone was vacuous while the first registry row IS PlanRegionSettings.

- **glm20 round decisions** (fix round over the 12 ledger-filtered findings of code-review round 20, then an 8-lens verifier pass — 2 raw candidates converging on ONE defect, CONFIRMED on adjudication by two independent refuters and fixed as glm20-13):
  - **The float-form exponent bound is the INT-SAFE magnitude width** (glm20-1 + glm20-13, extends glm19-1): float_literal_is_lossy_integer() bounds the exponent string before any (int) cast — the old arithmetic saturated in the wrong direction ((int) of a 19+-digit digit string → PHP_INT_MAX → the sum widens to float → the (int) re-cast → PHP_INT_MIN), inverting the verdict for saturating exponents. The BOUND is load-bearing: only 19+-digit magnitudes clamp (to 18 nines, itself int-exact). A SMALLER bound (glm20-1's first clamp: >3 digits → 9999) corrupts the digit-shift arithmetic for mantissa padding that CANCELS a genuine exponent in [1000, 10^18): the padded exact spellings of 0.1 and 2^100/2^300 flip accept→reject AT THE LIVE WIRE ENTRY POINT (violating glm19-1's pinned exact-accept), and the padded inexact 1e50 spelling flips reject→accept (reopening the raw-wire-vs-decoded divergence glm19-1 closed). Do not re-flag the saturation fix, and do not narrow or widen the width without re-deriving the cancellation cases (all pinned at both the helper and the wire).
  - **Identity/stop-sequence encodability rides the attribution walk; the SHAPE halves stay eager** (glm20-8, extends glm13-11/glm15-5/glm16-7): the JsonEncodeGuard composites are split — reject_tool_call_identity()/reject_tool_result_identity()/reject_misshapen_stop_sequences() keep the null/empty/type rules eagerly with byte-identical messages; two EncodabilityNet segments (guard_prompt_tool_identities, guard_stop_sequences) carry the encodability attributions, composed per surface at the old eager positions (zai: stop, identities, system, sampling, declarations, text; zai_anthropic: identities, stop, text, system, declarations). glm16-7's pins hold plus two new per-surface identity/stop-first pins. Two documented flips: a MISMATCHED tool-result id now names the eager pairing rejection (the old test fixture's isolation trick is gone — a matched pair shares one id, so the walk names the call side), and unencodable identity + non-replayable args on one call part now names the args (the replay guard runs at mapping) — both reject typed pre-transport either way. The glm13-11 tool-result RESPONSE exception stays eager, untouched.
  - **The surface set rides one cross-file owner** (glm20-4, supersedes glm16-14's per-file registry and glm15-13's literal-list pin — documented at the superseded pins): Support\ZaiSurfaces::SURFACES is SDK-free and carries NO slugs (each surface's slug stays its settings class's CACHE_SCOPE, glm15-23's direction). zai.php's boot() and uninstall.php derive from it (uninstall through its owner chain, the constant read guarded by the chain's readiness — the GLM8 #15 broken-install fatal-avoidance path keeps holding); Plugin::PROVIDER_CLASSES and the live probe stay SDK-dependent and are PINNED to the registry by ZaiSurfaceLockstepTest (provider[i]'s PROVIDER_ID must be surface[i]'s CACHE_SCOPE). The uninstall file now states the surface classes ZERO times — stronger than glm16-14's exactly-once. The glm16-14 class-free literals stay separate BY DESIGN, untouched.
  - **The stream's tool-args diagnosis outranks the generic frame error** (glm20-2): has_malformed_tool_input() is checked before has_malformed_event() in the zai_anthropic stream parse; the GLM8 #5 JSON fallback is unaffected (its live scenario — a whole JSON body mislabeled as a stream — produces no tool blocks, so the flag cannot be set there). Combination-pinned.
  - **The aggregator's usage validation is one helper** (glm20-6): validated_usage_view() serves both usage-carrying frames in AnthropicSseAggregator; null return ≡ validator rejection (a non-null return is ALWAYS an array view — the strict rule rejects a JSON-null usage as not-an-object, so the null return is unambiguous). Source-pinned: exactly one failure_reason() call site in the aggregator.
  - **reject_unsupported() probes the ModelConfig object** (glm20-7): toArray() is null-filtered per member, so the getter's null IS the sparse array's absent key; the getter name derives from the key's camelCase (no parallel key→getter map). The former array-only falsy flavors ('0' topK, array() outputFileType) are gone WITH their input class — the DTO's typed setters cannot hold them (the GLM9-ref unreachable-by-construction reasoning).
  - **The unused-import scanner recognizes group-use** (glm20-3, extends glm17-8/9): openings are matched on the same token-masked view; members are unrolled from the MASKED statement bytes — a comma inside a blanked comment cannot hide a member (the fail-open shape the fixture pins); unterminated groups stay NEUTRAL (@lint owns unparseable files, the glm17 boundary). Do not move the member split onto the raw bytes.
  - **One delimiter-parameterized matcher** (glm20-10): wp_connectors_matching_delimiter_end() returns the closing offset or false; the brace wrapper wears the visibility spans' EOF over-approximation policy (the glm18-7 boundary) and the paren wrapper stays honest (false) — the old EOF-vs-false split is a stated per-wrapper POLICY now, not copy drift. Source-pinned to exactly one depth loop.
  - **Harness parity decisions** (glm20-9/11/12): wpdb::prepare() escapes only '$' in preg replacements — the addslashes/replace backslash COLLAPSE get_col()'s LIKE-to-regex conversion relies on is deliberately preserved (escaping the backslash would double every backslash in the harness's SQL); wiredZaiAnthropicModel() is THE zai_anthropic wiring helper (per-suite model() helpers are one-line delegates); current_time('mysql') mirrors the timestamp branch's gmt/offset arithmetic (no plugin caller uses the mysql form — latent-safe).
  - **glm20-5**: both aggregator headers and one inline comment stop claiming the glm19-11-deleted malformed counter (the glm19-13 class, missed at the file headers).

- **glm19 round decisions** (fix round over the 12 findings of code-review round 19, then a 7-lens verifier pass — 11 raw candidates, 2 CONFIRMED on adjudication and fixed as glm19-13/14, 9 refuted with empirical repros and ledger citations):
  - **Float-form spellings join the raw-wire exactness oracle** (glm19-1, extends glm12-8/glm18-1): the plain-integer scan's e/E/./- adjacency guards hid every float token from the rule, so the exponent spelling of a lossy beyond-int integer accepted where its plain twin rejects. A second scan reconstructs each float-form token's exact decimal integer expansion and applies the same %.0f oracle. Do not re-flag: exact float forms still accept (9223372036854775808e0/…808.0/…808e18, 1e20, 1e22, 2^53 spellings); integral-but-inexact big floats (1e23, 1.5e25, the short DBL_MAX spelling 1.7976931348623157e308) REJECT because the literal's exact integer expansion is not the double — true precision loss per the same oracle, and the decoded walker already rejected those doubles (verifier-confirmed both directions); a float-form token's exact-integer guarantee ends at 2^53 (9007199254740993e0 is lossy where the plain literal decodes to an exact int); the conservative decoded half of glm12-8 is untouched.
  - **The malformed-body rewrite covers the vendor InvalidArgumentException family** (glm19-2, extends glm13-7/glm14-2): a USER-role choice message makes the vendor Candidate() constructor throw InvalidArgumentException — now caught with the ResponseException and rewritten to the fixed malformed-payload message on all three zai transports; on the mislabeled-JSON fallback the rewritten rejection is the non-marker family JsonFallbackResult nulls out, keeping the stream-typed error. Every plugin-thrown InvalidArgumentException is outbound (verifier-traced, all 26 sites) and never runs under the parent's parse; the FixedMessageResponseException marker still passes through. Do not re-flag the multi-catch as diagnostic-laundering.
  - **The replay stamp's defensive-path proof is associative-sibling-scoped** (glm19-3 + glm19-14): the caller-built ASSOCIATIVE sub-path (null raw oracle) now proves replayability through the FULL is_replayable() oracle before the GLM12 #12 stamp; the stdClass sub-path under the same branch stamps on the AGGREGATOR's word — every production null-raw caller is the aggregator channel (verifier-traced: 1667/1712/1763), whose input was replay-validated at acceptance precisely/conservatively per glm12-8, and conservative re-validation there would reject the exact big literals the precise rule accepted. The residual latent shape (a caller-built stdClass with a null raw view would stamp unvalidated) is DOCUMENTED at the sub-path and PINNED as deliberate (the boundary test asserts the stdClass channel stamps 1e20, which the conservative walker rejects). Do not propose closing it with is_replayable()/is_replayable_decoded() on the stdClass path.
  - **PreDecodedResponse forwards provenance** (glm19-4): the constructor takes the WHOLE original Response (status, headers, body ride verbatim); the vendor parse reads getData() only, so the forwarding is inert to parsing. Do not re-add a synthetic empty view.
  - **RequestShapeGuard owns the five twin request rules; messages interpolate PROVIDER_LABEL** (glm19-5/6, follows glm15-23's direction): empty/duplicate tool name, list-root parameter schema (non-empty list boundary), list-root output schema ([] rejects), maxTokens-positive live on the shared label-parameterized guard; both surfaces' messages ride the constant chain (REFUSAL_LABEL → CACHE_SCOPE), byte-identical to the old literals. Two same-shape literals remained in ZaiAnthropicRequestAuthentication (out of the round's reviewed scope) — folded by glm24-3 (see the glm24 round; the deferral is closed).
  - **The directory cache alias constants are deleted** (glm19-10, supersedes GLM4 #10's alias-agreement pins — documented here per the GLM10 #4 lesson): the eight production-dead mirrors are gone, the 37 test references ride the real owners (ZaiDiscoveryCache for TTLs/suffix, the settings classes for the prefix), and no-redeclaration anti-regression pins hold both directories. Do not re-add the aliases; do not flag the ZaiDiscoveryCache owner docblock (glm19-13 records the deletion there).
  - **The SSE aggregators' observability API is gone** (glm19-11): is_done()/event_count()/malformed_count() deleted from both aggregators together with the dead-store counters (zai $malformed; twin $events/$malformed); the load-bearing fields stay (zai $done/$event_count gate aggregated(); the twin's $terminated gates the frames). Termination/event pins read state via the harness reflection helper (aggregator_state()); malformed-count pins became behavioral assertions. Do not re-add public observability API to the aggregators.
  - **Plugin::PROVIDER_ID is deleted** (glm19-12): the docblock-advertised constant orphaned by the PROVIDER_CLASSES rewire; the provider classes' metadata is the registry's ID source.
  - **Verifier-round fixes** (glm19-13/14): docblocks claiming deleted behavior rewritten (the DISCOVERY_TTL present-tense alias claim, the absint()/event_count() narration drift); the stdClass trust boundary stated and pinned (see glm19-3+14 above).

## Verification-protocol decisions

- **Verification verdicts come from transcripts, not subagent vote tallies** (GLM1 lesson): the 12/3 CONFIRMED/PLAUSIBLE tally was corrected from a 9/2/4 vote read.
- **Empiricism over introspection** (GLM1 #14 lesson): wire-format claims are settled by live capture (curl), not model self-knowledge.
- **Test pins may be consciously superseded** (GLM10 #4 lesson): when an invariant has been extended twice (glm9-16), superseding the older pin is correct — document the supersession in the CHANGELOG.

## Drift-guard scope (post-GLM11)

- The label drift guard covers the model + discovery paths (ZaiModelListParser, both metadata directories, availability base). The **endpoint layer deliberately brands UNKNOWN_ENDPOINT_LABEL with 'z.ai'/'z.ai Anthropic'** (ZaiEndpoint.php:123, ZaiAnthropicEndpoint.php:150) — that is product naming, not drift; do not flag.

## Known deferred items (improvements, not bugs)

- **glm17 verifier-round boundaries (all pre-existing, none reachable in the repo today, verifier-confirmed)**:
  - **Inline-HTML column-0 use lines stay unmasked**: wp_connectors_mask_string_contents() passes T_INLINE_HTML verbatim, so a column-0 `use ...;` line in template HTML after `?>` still phantom-flags (identical behavior in the pre-glm17-8 raw-regex scanner — not a regression). 0 of 50 connectors/ files contain a close tag, and masking inline HTML in the SHARED masker would change the glm15-2 analyzer's views — a design decision, not a scanner patch. Re-open only when a connectors/ file legitimately carries template HTML.
  - **A column-0 trait-use statement phantom-flags as an unused import** (`use SomeTrait;` at column 0 inside a class body): pre-existing raw-regex behavior, and phpcs WordPress-Core rejects unindented class-body statements, so the shape cannot land while `composer phpcs` gates the tree. Re-open only if the phpcs gate is dropped.
  - **An unbalanced string delimiter collapses the masked view's tail to spaces** (a botched merge leaves a bare quote; every use statement after it is invisible to the masked scan and the STANDALONE `composer conventions` run stays green): the full `composer check` pipeline runs `@lint` (php -l over all sources) BEFORE `@conventions`, which owns exactly this failure class. Do not add an in-scanner parseability probe for it — that duplicates the lint step's job.
- **SseFrameBuffer::feed() incremental-scan cursor deferred** (glm15-24, a PLAUSIBLE latent finding accepted with its bound documented): each feed() re-normalizes and rescans the ENTIRE unconsumed buffer, so incremental chunk feeding is O(k · tail) — quadratic in the unconsumed tail, never in the whole stream. LATENT: both production callers (both model parsers) feed the complete body in ONE call. A persistent normalized/scanned cursor is the fix shape; deferred because the prefix-strip states rewrite the buffer head and the CR-hold rewrites its tail (every cursor transition is regression surface in the most heavily pinned support class) for zero current benefit. Do not re-flag the latent bound without an incremental consumer or a demonstrated slow path; the docblock at feed() is the honest statement.
- GLM2's 5 minor cleanup items + latent #8 (duplicate settings errors masked by wp_die) — deferred by user triage.
- GLM8-perf trio (negative-caching conflict with M1 decision; /models parsing duplication was since fixed in glm1-13) — superseded or deferred.
- GLM9 cut-over-cap items: credential/hook/render twins, is_object_shape() vs JsonShape::is_list() (unified glm9; the zai_anthropic copy joined it in glm16-9), triple property_exists, usage-oracle duplication (since unified), whole-payload encode oracle on zai streamed path (since swapped glm10-10), per-call chat-ID set rebuild, ~15 source-pinning tests, $ships_forwarded_values hand-flag, live probe's nine surface ternaries (since table-ized glm10-15).

Ledger maintained by the review loop; update when a decision changes, not when a reviewer disagrees.
